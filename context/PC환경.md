# PC 환경

_최종 시스템 확인: 2026-08-03 / 현재 업무 PC_

## PC 프로필 판정

- 현재 PC는 로컬 마커와 `scripts/pc_profile.ps1`로 `work` 프로필을 판정한다.
- 프로필 마커의 정본 위치는 `D:\ToolData\Harness\pc-profile.json`이며 저장소에는 PC별 값을 넣지 않는다.
- 프로필별 하네스·사용자 전달 산출물 경로는 `PATHS.md`를 정본으로 사용한다.
- 저장소 실제 경로와 호환 junction은 두 PC에 모두 존재할 수 있으므로 환경 식별 근거로 사용하지 않는다.
- 새 PC의 마커가 없거나 잘못됐으면 `unknown`으로 판정하고, 사용자 확인 없이 업무용·개인용 중 하나로 추정하지 않는다.
- 마커에는 `work`/`personal`만 기록하며 호스트명·계정명·하드웨어·네트워크 식별자를 사용하지 않는다.

## 하드웨어와 운영체제

| 항목 | 확인값 | 상태 |
|---|---|---|
| 운영체제 | Windows 11 Home 64비트, 버전 `10.0.26200`, 빌드 `26200` | 시스템 확인 |
| CPU | Intel Core i9-14900HX, 24코어·32논리 프로세서 | 시스템 확인 |
| 메모리 | 31.7GiB, 실사용 기준 32GB급 | 시스템 확인 |
| 그래픽 | Intel UHD Graphics + NVIDIA GeForce RTX 4060 Laptop GPU | 시스템 확인 |
| PowerShell | Windows PowerShell `5.1.26100.8875` | 시스템 확인 |
| C: 드라이브 | 전체 475.9GiB, 여유 390.0GiB | 2026-07-29 스냅샷 |
| D: 드라이브 | 전체 476.9GiB, 여유 453.5GiB | 2026-07-29 스냅샷 |

GPU 메모리는 Windows WMI 값이 실제 전용 메모리를 정확히 표현하지 않을 수 있어 기록하지 않았다. 장치명·계정명·일련번호·네트워크 식별자는 수집하지 않는다.

## 환경 해석

- **추론:** CPU·메모리·GPU 기준 고성능 개발 노트북이다. Java 빌드, IDE, DBeaver, 브라우저 자동화와 일반적인 병렬 검증을 저사양 제약 없이 수행할 수 있다.
- **추론:** 32GB 메모리는 현재 스택에 충분하지만 여러 IDE·브라우저·Gradle·로컬 DB를 동시에 장시간 실행할 때는 실제 메모리 사용량을 측정한 뒤 병렬도를 정한다.
- Codex 모델 추론 품질은 클라우드 모델 설정의 영향이 크다. 이 PC 사양은 주로 로컬 빌드·테스트·브라우저 자동화·도구 동시 실행 판단에 사용한다.

## 저장소와 데이터 경로

- 현재 PC 프로필: `work`
- 현재 하네스 루트: `D:\harness_framework`
- 사용자 설치 개발 도구 정본: `D:\Tool`
- 변경 가능한 작업 도구 데이터 정본: `D:\ToolData`
- 로컬 실행 로그 정본: `D:\LOGS`
- 과거 `E:\harness_framework`와 개인 PC의 `D:\tomes\harness-framework` 기록은 현재 업무 PC에서 그대로 실행 경로로 사용하지 않는다.

세부 경로와 제품 하위 구조는 `PATHS.md`를 정본으로 사용한다.

## D: 권한 해석

- 일반 Windows 사용자 권한 기준으로 D: 루트는 `Authenticated Users` 수정 권한이 있어 현재 사용자가 쓸 수 있다.
- Codex의 Windows 샌드박스는 일반 사용자 토큰과 다른 제한 토큰·ACL 경계를 사용한다. C: 사용자 프로필에는 샌드박스 접근 ACE가 있고 `D:\harness_framework`에는 `CodexSandboxUsers` 수정 권한이 별도로 있어 쓰기가 가능했다.
- 기존 `D:\Tool` 밖의 새 D: 폴더에는 샌드박스용 ACE가 없어 일반 앱은 쓸 수 있어도 Codex 도구 호출은 접근 거부될 수 있었다.
- `D:\ToolData` 한 곳은 D: 루트의 넓은 `Authenticated Users` 상속을 끊고 현재 사용자·SYSTEM·관리자는 전체 권한, `CodexSandboxUsers`는 수정 권한만 하위 상속하도록 제한했다. D: 전체 권한은 변경하지 않는다.

## 재확인이 필요한 경우

- 개인 PC에서 작업할 때의 하드웨어·도구 차이
- OS 업데이트, 메모리 증설, 저장장치 변경
- 대형 병렬 작업에서 실제 CPU·메모리·디스크 병목이 관찰될 때

현재 작업 결과가 위 정보에 따라 달라질 때만 다시 조사하거나 사용자에게 확인한다.
