# 기타 — 상세 인수인계

_2026-07-14 HANDOFF.md 작업별 분리로 생성. 범위: 도면팝업 CSS 분리·백업/application-local.yml 복구·getCompanyStaffList 캐시 개선. 섹션 최신순, 원문 그대로 이동._

## [2026-08-11] %클라우드 작업트리 주제 분리
- `feature-control-0713` 작업트리에서 소재·표면처리 사용 여부·캐시와 무관한 `build.gradle`의 JUnit Platform 활성화 변경을 별도 stash 객체 `b75b9c07d25904b604ff6b4980860c28287b7816`로 분리했다.
- stash의 대상은 `build.gradle` 1파일이며 분리 전후 patch-id `185f2f85dbdd6626d02e7eb402ab3f4e58d40521`이 일치한다.
- 가공확정 KAN-63·KAN-6 변경 7파일은 통합 stash 객체 `d2c8a638e1f508bd4672b47fc191245061c2c02b`에 보관돼 있다. `.claude/settings*.json`은 로컬 환경 설정이라 기능 stash에 포함하지 않았다.
- 분리 후 남은 tracked 변경은 테넌트 D01·D03 사용 여부와 캐시 무효화 관련 9파일뿐이며, 해당 diff Git blob 해시 `9918fcd3fdb23b0f0238e859051e19c2939c8b77`과 재현 SQL SHA-256 `1BC9C0AD3B084A39CAFF852470F27AFC802002E91DBA27BA9FE1F4C86B8A4D6C`가 분리 전후 동일하다. `git diff --check`도 통과했다.

## [2026-07-08 밤] 도면 팝업 인라인 스타일 -> bottom-drawingInformation.css 이전 (%클라우드, 미커밋)
- 사용자 요청: tabs/bottom.jsp의 drawingInformation 관련 태그 인라인 스타일 전부를 `publishing/bottom-drawingInformation.css`로 이전.
- 이전 완료: `#common_quick_drawing_popup`(Drawing Information 팝업, 줄 1011~1046) 인라인 style 10건 전부 CSS로 이동. 이름 없던 래퍼 div 4개에 클래스 부여(quick_drawing_body/img_area/info_area/btn_area). JSP 상단 `<style>` 블록(numInput 등)은 무관해 유지.
- ★맥락: 워킹트리에서 `publishing/bottom.css`가 이미 삭제(D)되고 동일 내용의 `bottom-drawingInformation.css`(untracked)로 개명돼 있었음(세션이 한 것 아님, 링크 갱신은 누락 상태였음). 이번에 bottom.jsp 줄 7의 죽은 bottom.css 링크를 새 CSS 링크로 교체해 개명을 완결.
- 동작 안전성 확인: bottom.js/body-script의 .show()/.hide()가 인라인 display를 설정하므로 CSS display:none과 공존, ID 셀렉터 5종은 webapp 전역 고유. answer-reviewer 검증 통과(스타일 값 10건 원본 일치 확인). 실화면(팝업 열기) 검증은 미실시 — 서버 내려가 있음.
- 수정 파일: tabs/bottom.jsp(M), bottom-drawingInformation.css(untracked, 내용 확장). 커밋은 사용자 몫(현 체크아웃 main 주의).

## [2026-07-08] 백업본(스태시/패치) 복원 + application-local.yml 유실 복구 (%클라우드, 미커밋)
### 복원 (사용자 지시 "문제 안 되는 부분 가져와")
- 원본: stash("pull 전 로컬 수정 4파일 보관")과 동일 내용인 `logs/cloud_local_changes_backup_2026-07-08.patch`. **stash는 건드리지 않고 패치로 적용**. (→ 7/20 해당 stash는 사용자 승인 하에 drop됨 — 거래명세표 분은 새 stash@{0}으로 재구성, 백업 패치 `logs/stash_backup_2026-07-20_stash1_4files.patch` 추가 보존. 상세 handoff/거래명세표.md)
- 3파일 무수정 적용: estmateCalculate.xml(SF_GET_ETC_NM 3인자 수정), v1/estmateCalculate.xml(MATERIAL_SUPPLY_YN 컬럼), estimate_unit_price_calculate.jsp(목업 제거+기타 플래그 배선).
- transaction_statement.jsp는 일반 적용 실패 → **3-way 적용 성공, 충돌 0**: 거래명세표 슬라이드36+라벨모달 작업과 main 퍼블리싱 변경(아이콘 클래스 개명 2건: shareIcon→icon-link-external, fileSearchIcon→icon-img-search) 공존 확인. ※개명된 신규 아이콘 클래스와 복원된 UI의 시각 검증은 미실시.
- 전부 워킹트리 미커밋(%클라우드 커밋 사용자 몫). 검증: 4파일 diff 존재+핵심 grep 확인, 8081 재기동 성공. stash와 워킹트리 diff 대조 = 3파일 동일, transaction_statement.jsp만 아이콘 개명 2건 차이(의도된 3-way 결과).
- ★현재 체크아웃 = **main**(머지 작업 후 Danga-order로 복귀시켰으나 이후 사용자가 main으로 전환한 것으로 추정, 세션이 한 것 아님). main==Danga-order==93211d4 동일 커밋이라 복원 내용 영향 없음. 커밋 시 브랜치 선택 주의.
### ★ application-local.yml 유실 복구 (신규 이슈)
- 재기동 시 `common.security.crypto-key` 플레이스홀더 미해결로 기동 실패 → 조사 결과 **src/main/resources/application-local.yml 파일 자체가 디스크에서 사라져 있었음**(6c37383에서 추적 해제된 gitignore 파일 — 07-08 사용자 정리 과정에서 삭제된 것으로 추정, 경위 미확인).
- 복구: 마지막 추적 버전(`6c37383~1`) blob을 그대로 추출 + **JSP 핫리로드 키 위치 교정만 반영**(server.jsp→server.servlet.jsp, 06-30 교정 사항 [[feedback_auto_server_restart]]). crypto-key는 develop/production yml과 동일값이 이 버전에 이미 포함.
- 주의: 06-16(추적해제)~07-08 사이 사용자가 로컬에서 추가 수정한 내용이 있었다면 이 복구본엔 없음 — 이상 동작 시 이 파일 먼저 의심.
- 8081 재기동 OK(3.7s, /login 302, logs/bootrun_8081_0708c.log). 기존 서버 PID 35644는 종료함.

## [대기중] %클라우드 getCompanyStaffList 캐시 개선 (사용자 트리거 대기)
- 사용자가 %클라우드를 `feature_caching_2606` 브랜치로 바꾼 뒤 알려주면 착수. (현재는 `Danga_estimate`, 견적 8파일 미커밋 상태 — 사용자가 먼저 커밋/stash 후 전환 예정)
- 작업 내용: `getCompanyStaffList`(data_source.xml 26~37행)를 서비스 계층에서 **COMP_CD 무시하고 전체 조회 → sid 단일키 캐시 → COMP_CD 있으면 메모리 필터**로 리팩터. 근거: 호출부 다수가 COMP_CD 없이 전체 조회 후 클라에서 compCd 필터(create_order/control/stock, cost-analysis, order/control-manage, estimate-list 등), COMP_CD 지정은 소수 동적 호출(transaction_statement 311행, estimate-regist 1599·1630행)뿐 → 원 분석의 키 카디널리티 우려 과장. 키를 테넌트당 1개로 수렴.
- 같이 처리 권장: selectCompList 캐싱 유지/제외 결정 + 런타임 캐시 적중/evict 실측(redis_cache_impl_report_2026-06-22.md §7).
- 캐시 작업 본체는 이미 `feature_caching_2606`에 커밋됨(HEAD b504ace). %클라우드 커밋은 사용자 몫 — 말없이 커밋 금지.
