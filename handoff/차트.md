# 차트 — 상세 인수인계

_2026-07-14 HANDOFF.md 작업별 분리로 생성. 범위: 메인화면 작업자별 가동율 차트 (V1 적용 완료분은 아카이브, 잔존 표시/구조 이슈). 섹션 최신순, 원문 그대로 이동._

## [2026-07-03] 메인화면 작업자별 가동율 차트 이슈 분석 (→ 2026-07-09 기능 이슈 ①②③은 V1 쿼리 적용으로 해소, 상단 섹션 참조. 표시/구조 이슈만 잔존)
### 구조
- 메인 탭 = `/static/main/main`(StaticUrlController.java:79) → `views/pages/main/main.jsp`. 차트는 Highcharts 아닌 **수제 CSS 막대**: `loadChartDataList()`(main.jsp:307) → `main.selectOperatingRateChart`(main.xml:6-41) → `refreshChart()`(main.jsp:249)가 HTML 조립해 `.vGraph_ul`에 append.
### 기능 이슈 (프런트 재작업과 무관하게 유효 — 지금 수정 제안 범위)
- ①SQL 테넌트 필터 조건부: SYSTEM_ID 조건이 FACTORY_AREA 지정 분기(main.xml:28-33) 안에만 있어 "구역=전체"(기본값) 조회 시 테넌트 필터 전무. v1 쿼리엔 있었음. 타 테넌트 노출·1000테넌트 스캔 부하 리스크.
- ②Average 왜곡(추론): TBL_BATCH_MCT_WORK_EQUIP INNER JOIN이 작업자 수만큼 행 증식 → ROLLUP Average가 작업자수 가중평균으로 왜곡. v1은 IN 서브쿼리로 회피했었음.
- ③JS null 크래시: `rowData.EQUIP_NM.toLowerCase()=='average'`(main.jsp:268), EQUIP_NM null이면 TypeError로 루프 중단=차트 일부만 그려짐. 음수/NaN REAL_RATIO 방어도 없음.
- (부수) 죽은 바깥 LEFT JOIN TBL_EQUIP B(main.xml:38).
### 표시/구조 이슈 (보류 — 프런트 방향 결정 후)
- 막대 뜸: 가동률 14% 미만 시 top=88% 강제인데 height는 비율% 그대로(main.jsp:258-265) → 밑면이 바닥에 안 닿음.
- SQL이 HTML 생성: CONCAT(EQUIP_NM,'<br> (',...)(main.xml:10) + JS 이스케이프 없는 결합(266-274행) → 지금 빼면 화면 깨져 보류, 개편 시 순수 컬럼 분리로 이행.
- 픽셀 하드코딩 상충: 인라인 125px / .vGraph 172px·ul 180px·li 150px(common.css:715-719) / h_40(부모40%) / 라벨 ±35px(common.css:722-723) → 화면설계서 10번 슬라이드 ②(그래프 상하 축소) 반영 시 전면 재작업 필요.
- 죽은 Highcharts 게이지 코드(gaugeOptions 175행/createChart 288행 등, 호출 0곳): 퍼블리셔가 Highcharts로 갈 수 있어 **삭제 보류**(출발점 재료).
- 자잘한 중복: #main_aggregation_dt_button 핸들러 2회 등록(419/725행), 막대 클릭 핸들러(708행)=loadLeftGrid(358행) 복붙.
### 다음 단계 (제안만 하고 세션 종료됨 — 미진행)
- 제안 범위: **SQL 3건**(SYSTEM_ID 상시 적용 + INNER JOIN→IN 서브쿼리 + 죽은 LEFT JOIN 제거, queryId 유지·응답 JSON 구조 불변) + **JS 방어 2줄**(EQUIP_NM null 가드, REAL_RATIO 클램프). 검증: 빌드→8081 재기동→구역/작업자 조합별 전후 값 비교(Average 변화는 교정임을 명시).
- 변수: 퍼블리셔가 Highcharts로 개편할 수도, 사용자가 직접 할 수도 — 기능 이슈만 먼저 사용자 측에서 수정 의향.
