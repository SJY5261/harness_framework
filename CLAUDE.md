# 프로젝트: Tomes-Cloud

## 프로젝트 약칭 (대화 지칭 규칙)
사용자가 아래 약칭으로 말하면 해당 디렉토리를 가리키는 것으로 해석한다. 답변에서도 이 약칭을 사용해도 된다.
- `%클라우드` → `E:\harness_framework\Projects\Tomes-Cloud` (Spring Boot 본 애플리케이션)
- `%테스트` → `E:\harness_framework\Projects\Test-Automize` (최신 테스트 자동화 프로젝트. 구버전 `E:\Tomes-AutoTest`는 삭제됨)
- `&하네스` → `E:\harness_framework` (하네스 프레임워크)

※ 2026-06-12 디렉토리 재구성: 두 프로젝트가 `E:\Tomes-Cloud`, `E:\Test-Automize`에서 `&하네스\Projects\` 하위로 이동됨. 구 경로가 적힌 과거 문서·로그는 새 경로로 읽는다. 각 프로젝트는 자체 git 저장소를 유지한다(하네스 저장소와 별개).

※ 개인 PC 기간(2026-07-23~, 업무용 노트북 수리): 이 PC의 하네스 루트는 `D:\tomes\harness-framework`이고 E: 드라이브가 없다. 이 파일·PATHS.md·과거 문서·아래 명령어 예시의 `E:\harness_framework`(`E:/harness_framework`)는 전부 `D:\tomes\harness-framework`로 읽는다(하위 구조 동일 — `Projects\Tomes-Cloud`, `Projects\Test-Automize` 존재 확인됨). 업무용 PC 복귀 시 이 문단을 제거한다.

## 기술 스택
- Java 21 + Spring Boot 3.5
- MyBatis (SQL 매퍼, XML 기반)
- JSP + JSTL (뷰 레이어)
- ParamQuery Grid 11.0.0 (UI 그리드)
- MariaDB (DB), Redis (세션/캐시)
- Gradle (빌드), WAR 배포

## 아키텍처 규칙

### CRITICAL — 절대 위반 금지
- CRITICAL: MyBatis XML의 기존 queryId를 변경하지 마라. 컨트롤러·서비스에서 문자열로 직접 참조하므로 변경 시 런타임 오류 발생.
- CRITICAL: application-*.yml에 신규 DB 비밀번호·JWT 시크릿·API 키를 하드코딩하지 마라. 기존 하드코딩은 유지하되 신규 추가 시 금지. (보안 정책 미확정으로 추후 변경 가능)
- CRITICAL: ParamQuery Grid는 반드시 11.0.0 API를 사용하라. 7.x 문법 사용 금지.
- CRITICAL: JSP 수정 시 연결된 Java 컨트롤러의 메서드 시그니처·URL 매핑을 임의로 변경하지 마라.

### 일반 규칙
- SQL 매퍼 XML은 sqlMaps/ 하위에만 추가하고, 새 XML 파일을 무단 생성하지 마라.
- 비즈니스 로직은 ServiceImpl에, DB 접근은 DaoImpl에만 작성한다. 컨트롤러에 로직을 넣지 마라.
- 기존 Test-Automize 테스트를 깨뜨리는 변경을 하지 마라.
- 지시된 범위 외 추가 구현·리팩터링을 하지 마라.
- 작업 전 관련 파일(Controller, ServiceImpl, DaoImpl, XML, JSP)을 모두 읽고 흐름을 파악한 뒤 수정하라.

### 확장성(테넌트 스케일) 고려 규칙
- 기능을 변동(추가·삭제·수정)할 때는 **테넌트 1000개 이상 규모에서도 정상 동작·확장되는지**를 항상 함께 검토한다. 당장은 영향이 없어 보여도 테넌트·데이터·트래픽이 늘면 부하가 누적되는 설계인지 미리 따진다.
- 점검 관점(최소): ①요청·쿼리 빈도가 테넌트 수에 비례해 선형/초선형으로 증가하는가(N+1, 페이지·세션마다 반복 조회 등) ②캐시 키 카디널리티가 테넌트 수만큼 폭증해 메모리/Redis를 압박하는가, 반대로 키가 과도하게 공유돼 테넌트 격리가 깨지는가 ③테넌트 전체를 한 번에 도는 풀스캔/전역 락/일괄 작업이 끼어들지 않는가 ④인덱스·페이지네이션·배치 없이 전체 행을 적재하지 않는가.
- 설계 시 **테넌트 단위 격리·증분 처리·적정 캐시 키·인덱스/페이지네이션**을 기본 전제로 삼는다.
- 우려가 있으면 변경을 진행하더라도 **스케일 리스크와 대안을 답변·HANDOFF에 명시**한다. 확신이 없으면 단정하지 말고 부하 가정·근거를 함께 보고한다.

## 자율 실행 안전 규칙
- %클라우드(`Projects\Tomes-Cloud`) git 커밋은 전적으로 사용자 몫이다. 절대 말없이 커밋하지 마라. 사용자가 해당 턴에 명시적으로 지시한 경우에만 수행한다. (자동화 세션·하네스 step 세션도 동일 — execute.py 프롬프트 규칙 6 참조)
- &하네스(`E:\harness_framework` 저장소 자체) git 커밋·푸시는 Claude가 알아서 처리한다(2026-07-21 사용자 지시). 무엇을 바꿀지(내용 결정)는 여전히 사용자 몫이고, 그 변경을 커밋·푸시로 이력에 남기는 절차는 매번 묻지 않고 수행한다. 단, 변경 파일 범위는 지시된 작업과 직접 관련된 것으로 한정하고(관련 없는 다른 미커밋 작업을 임의로 쓸어 담지 않는다), 무엇을 커밋했는지는 답변에 사실대로 보고한다. force-push·리베이스 등 파괴적 작업은 이 위임에 포함되지 않으며 별도 확인을 받는다.
- 파일을 삭제하기 전에 반드시 삭제 이유와 대상 경로를 보고한다.
- 작업 지시에 명시된 파일 외에 다른 파일을 수정하지 마라.
- %클라우드 코드 작업 시 `E:\harness_framework\Projects\Tomes-Cloud` 외부 디렉토리의 파일을 수정하지 마라. (단, 하네스 관리 파일인 `phases/` 상태 갱신과 `HANDOFF.md`는 예외)
- DB 데이터를 직접 삭제(DELETE 전체, TRUNCATE)하지 마라.
- DB UPDATE·마이그레이션 전에는 반드시 ①대상 백업(백업테이블 CREATE 또는 before-image 저장) ②트랜잭션으로 감싸기(START TRANSACTION) ③실행 후 검증 SELECT를 거친다. 운영 DB는 검증까지 마친 뒤 사용자 확인을 받고 COMMIT한다. 자의로 COMMIT하지 마라.
- 확신이 없으면 실행하지 말고 현재 상태를 보고한 뒤 지시를 기다린다.
- 설정 변경(settings.json, 권한 모드 등) 같은 민감한 작업은 yes/no를 묻기 전에, 무엇을·어떻게·왜 바꿀지 답변 본문으로 상세히 설명한 뒤 확인을 받는다. (권한 다이얼로그/툴 호출의 짧은 설명만으로는 파악이 어려우므로)

## 세션 관리 규칙
- HANDOFF 구조 (2026-07-14 작업별 분리 개편): `&하네스\HANDOFF.md`는 **인덱스**다 — 공통 상태(브랜치·미커밋 파일·서버·전역 주의)와 작업별 한 줄 엔트리(상태+다음 액션+상세 파일 포인터), 규칙 변경 공지 같은 큰 내용만 담는다. 작업 상세(경위·검증 상태·다음 스탭·관련 파일)는 `&하네스\handoff\<작업주제>.md`에 작업별로 기록한다.
- 세션 시작 시 인덱스(HANDOFF.md)만 읽고, 지시받은 작업의 상세 파일만 추가로 읽는다. 갱신도 해당 작업 상세 파일 + 인덱스 한 줄만 한다. 인덱스에 작업 상세를 다시 쌓지 마라(토큰 절약 규칙과 동일 취지).
- 작업 분기점마다, 그리고 세션 종료 전에 해당 작업의 상세 파일과 인덱스 줄을 갱신해 다음 세션이 인수인계받게 한다. 상세 파일 포맷: 작업 목표 / 완료 / 진행중·미완 / 다음 단계 / 블로커·주의 / 관련 파일·경로.
- 종결된 작업은 상세 파일 내용을 `logs/HANDOFF_ARCHIVE.md`로 이동(삭제 아님)한 뒤 상세 파일과 인덱스 줄을 제거한다. 과거 이력 보존은 일별 worklog(logs/)·Notion 작업 트래커·git 이력이 담당하므로 handoff에 중복 보관하지 않는다.
- 컨텍스트 분량 관리(50% 임계치 등)는 사용자가 플러그인으로 직접 관리한다. 모델은 관여하지 않는다(아래 토큰 절약 규칙의 착수 전 초기화 "제안"만 예외). 사용자가 "초기화"라고 하면 즉시 HANDOFF 최종 갱신 후 `/clear`를 안내한다.

## Claude x Codex 협업 규칙
- 공유 정본은 기존 `HANDOFF.md` + `handoff/` 체계다. Claude memory는 공유 정본이 아니다. 장기 결정은 해당 `handoff/<작업>.md`의 `결정` 섹션에 기록하고, 작업 종결 시 `logs/HANDOFF_ARCHIVE.md`로 이동하며, 여러 작업에 적용되는 규칙만 `CLAUDE.md`로 승격한다.
- 교차 토론은 설계 갈림길, 회귀 위험이 큰 변경, 보안·데이터 손실 가능성이 있는 비가역 변경, 또는 사용자의 명시적 요청에만 사용한다. 단순 구현과 명백한 수정은 한 에이전트가 수행하고 다른 에이전트가 교차 리뷰한다.
- 토론 시작 시 `discussion/TEMPLATE.md`를 복사해 `discussion/YYYY-MM-DD_<주제>.md`를 만든다. 같은 날짜·주제 파일이 있으면 `_<discussion_id>`를 붙인다. 토론 원문은 종결 후에도 `discussion/`에 영구 보존하고, synthesis 요지만 해당 handoff에 반영한다.
- Claude가 오케스트레이터다. Codex 호출은 `codex exec --sandbox workspace-write --cd E:\harness_framework`처럼 최소 권한과 명시적 작업 경로를 사용한다. 승인 우회 옵션을 임의로 쓰지 않으며, 프롬프트에는 문제문·필요한 정본 경로·수정 허용 구간·완료 조건을 독립 실행 가능하게 포함한다.
- 블라인드 Round 1은 문제문만 파일에 고정하고 Claude 의견은 세션 안에서 확정한다. Codex에는 같은 문제문만 전달하며, 양측 의견이 모두 확정된 뒤 각 소유 구간에 함께 공개한다. Round 2부터 상대 의견을 공개해 교차 검토한다.
- 토론은 최대 3라운드다. 정확성, 저장소 근거, 구현·운영 비용, 회귀 위험, 검증 가능성을 고정 판정 기준으로 삼는다. 합의되지 않은 상대 의견을 삭제하거나 의미 변경하지 않고, 3라운드 후 미합의 또는 근거 부족 쟁점은 사용자에게 상신한다.
- 각 `codex exec` 전 토론 파일 원문을 기준본으로 보관하고, 호출 후 diff가 지정한 `BEGIN OWNER:CODEX`/`END OWNER:CODEX` 구간 안에만 있는지 확인한다. 구간 밖 변경이 있으면 진행을 중단하고 보고한다. 이 검사는 해당 호출의 안전 검사일 뿐 재개 상태의 정본이 아니다.
- 호출마다 타임아웃과 종료 코드를 확인하고, 정상 종료와 해당 소유 구간의 완성을 모두 확인한 뒤 다음 라운드로 간다. 실패 결과를 같은 구간에 중복 append하지 말고 템플릿의 실행 기록에 시도·사유·마지막 응답 위치를 남긴다. 재시도해도 해결되지 않거나 사용자 확인이 필요하면 상태를 `failed`로 바꾸고 사용자에게 상신한다.
- 호출 실패는 먼저 작업·콘텐츠성 실패와 에이전트 가용성을 나타내는 인프라성 실패로 분류한다. 프롬프트·파일 내용·응답 형식 등 작업·콘텐츠성 실패는 해당 토론만 재시도 정책에 따라 처리하고, 소진 시 failed로 상신하되 장애 대응 모드로 격상하지 않는다. 인증 실패·실행 파일 부재처럼 재시도로 회복될 수 없음이 명확한 인프라성 실패는 재시도 없이, 연결·서비스 일시 불가처럼 회복 가능성이 있는 인프라성 실패는 동일 조건으로 1회만 재시도한 뒤 같은 원인이 재현되면 사용자에게 single-agent-fallback 전환 여부를 확인한다. 사용자가 상대 에이전트의 사용 불가를 알린 경우에는 재시도 없이 전환한다. (2026-07-21 결함 보완 토론 합의, discussion/2026-07-21_협업체계_취약점보완.md)
- 종결 시 라운드·상태를 `completed`로 바꾸고 `synthesis 반영처`에 `handoff/<작업>.md` 상대 경로를 기록한다.

### 단일 에이전트 장애 대응 모드
- 한쪽 에이전트의 토큰 소진, 서비스 장애, 인증 실패 등으로 협업이 불가능하다고 사용자가 알리거나 실제 호출 실패로 명확히 확인되면 `single-agent-fallback`으로 전환한다. 토큰 잔량을 모델이 추정해 임의 전환하지 않는다.
- 사용 가능한 에이전트가 임시 오케스트레이터가 되어 기존 정본을 읽고 구현·검증·HANDOFF 갱신을 계속한다. Claude 사용 불가 시 Codex가, Codex 사용 불가 시 Claude가 맡는다. 상대 에이전트 호출 절차만 생략하며 나머지 저장소 안전 규칙과 사용자 승인 경계는 그대로 유지한다.
- 이미 합의된 설계, 가역적인 일반 구현, 명백한 결함 수정은 단독으로 진행할 수 있다. 새 보안 결정, 데이터 삭제·DDL·운영 반영, 되돌리기 어려운 변경, 합의되지 않은 대규모 설계 변경은 기존 사용자 승인 없이 장애 대응 모드가 권한을 넓혀 주지 않는다.
- 상대 관점이 결과를 바꿀 수 있는 설계 판단이나 교차 검증은 `peer_review_pending`으로 해당 handoff와 토론 파일에 기록한다. 이를 양측 합의 또는 교차 리뷰 완료로 표시하지 않는다. 단독 검증 결과와 미검증 범위를 구분해 남긴다.
- 진행 중인 토론에서 한쪽이 사용 불가해지면 협업 모드, 활성/사용 불가 에이전트, 전환 사유, 보류된 검토를 `discussion/TEMPLATE.md` 계약에 따라 기록한다. 응답 없는 상대 의견을 추정해 채우지 않으며, 3라운드 합의를 가장하지 않는다.
- 사용자가 상대 에이전트의 복구를 알리면 `normal`로 복귀한다. 복구된 에이전트는 `peer_review_pending` 변경만 저장소 diff·검증 기록과 대조해 일괄 리뷰하고, 결과를 handoff에 기록한다. 문제가 없으면 `peer_review: completed`, 수정이 필요하면 새 작업으로 분리한다. 리뷰 범위를 handoff 등 다른 문서에서 이 조항보다 좁게 재정의하지 않는다.
- `scripts/execute.py`의 무인 step 실행은 Claude 전용이며 자동 Codex 폴백을 지원하지 않는다. 실행 중 Claude 호출이 실패하면 execute.py는 현재대로 재시도 소진 후 중단·보고하며, 사용자가 장애 대응 모드 전환을 확인한 뒤에만 Codex가 대화형 세션에서 해당 step의 현재 상태와 변경 diff를 먼저 대조하고 나서 수동으로 이어받는다. 자동 폴백은 두 CLI의 출력·권한·재개 계약과 중복 실행 방지 검증이 마련될 때 별도 과제로 다룬다.

## 토큰 절약 규칙 (2026-07-13 도입 — 근거: 대형 자율 턴이 팽창한 컨텍스트에서 반복 호출되며 낭비 실측)
- 대형 파일(약 1,000줄 이상 JSP/XML/JS)은 전체 Read 금지. Grep으로 대상 구간을 먼저 특정하고 offset/limit 부분 읽기로 필요한 범위만 읽는다.
- 여러 파일을 훑는 탐색·전수조사·소비처 추적은 메인 대화에서 직접 하지 말고 Explore/general-purpose 서브에이전트에 위임해 결론만 받는다. 파일 덤프가 메인 컨텍스트에 눌러앉지 않게 하는 것이 목적.
- 대형 툴 출력 억제: 로그·빌드 출력·검증 스크립트 결과는 tail/head/grep으로 필요한 줄만 가져온다. 수천 줄 원문을 통째로 출력하지 마라.
- 3스탭 이상 예상되는 자율 작업은 실행 전에 스탭 목록을 제시하고 사용자 합의 후 진행한다(진행→전체 취소 왕복으로 인한 매몰 비용 방지).
- 대형 자율 작업 지시를 받았는데 현재 세션 컨텍스트가 이미 크게 팽창해 있으면, 착수 전에 "HANDOFF 갱신 → 초기화 → 새 세션에서 착수"를 먼저 제안한다. 제안만 하고 결정은 사용자 몫.

## 출력 포맷 규칙
- 줄 길이·글자 수 제한은 두지 않는다.
- 기본은 보통 글자(일반 텍스트·목록)로 답한다. 모든 답을 표로 만들지 마라.
- 사용자가 "표로 출력"하라고 할 때만 표를 쓴다. 표는 아래 확정 형식으로 그린다:
  - 박스 테두리(`┌ ─ ┬ ┐ │ ├ ┼ ┤ └ ┴ ┘`) + 일반 공백 패딩.
  - 한글 등 동아시아 폭(East Asian W/F) 문자는 2칸으로 계산해 열 너비를 맞춘다(정렬의 핵심 — 손계산 말고 스크립트로 생성하라).
  - 마크다운 표(`| |`)는 렌더러가 폭을 잘못 재 깨지므로 쓰지 말고, 위 박스 표를 코드블록에 넣어 출력한다.
  - 전각(全角) 문자로 칸을 채우는 방식은 쓰지 않는다(ASCII가 늘어나 보임).

### 검토 반영 답변 구분 배너 (필수)
answer-reviewer 검토를 거친 최종 답변은, 검토 전(초안·검토 과정)과
한눈에 구분되도록 아래 큰 배너로 시작한다. 배너 위는 검토 전,
배너 아래는 검토 후로 읽는다. ASCII만 사용(cp949 안전), 폭 ~40자 유지.
```
========================================
||||  검토 반영 답변 (REVIEW-VERIFIED)
||||  아래부터 검증을 거친 내용입니다
========================================
```

**검토 생략(옵트아웃):** 이번 답변에 신규 저장소 사실 주장이 없으면
(개념 질문, 이미 검증된 사실의 재진술 등) 검토를 생략하고,
답변의 최종 메시지에 한 줄 마커 `[검토 불요: 신규 저장소 사실 주장 없음]`를
전체 문구 그대로 넣는다. 백스톱 훅(answer_review_gate.py v2)은 최종 메시지의
전체 마커만 인정하고, 검토를 거친 답변은 최종 메시지에 배너가 있는지도 확인한다.
남용 금지 — 새 사실·범위 주장이 하나라도 있으면 반드시 검토한다.

## 산출물 파일 저장 규칙
사용자에게 보여줄 산출물(예시·목업·HTML/CSS/JS·이미지·스크립트·MD 등)은 scratchpad
임시 디렉토리에 만들지 마라. 아래 바탕화면 폴더에 저장한다(사용자가 다른 경로를 명시하면 그곳).
- MD 파일 -> `C:\Users\USER\Desktop\MD\<내용+날짜>.md`
- 기타 파일(html/css/js/이미지/스크립트 등) -> `C:\Users\USER\Desktop\예시\<주제>\...`
  (관련 파일 묶음은 그 아래 하위 폴더로: `예시\<주제>\`)
- PR 리뷰 -> 바탕화면 `PR리뷰` 폴더에 **작업별 폴더** `[MM.DD 요약]\`를 만들고 그 안에
  MD 정본 + 아티팩트 HTML + 링크(.url)를 함께 저장 ('/'는 '.'로). 상세: `&하네스\PR_작성규칙.md` 10절.
이유: 사용자가 산출물을 한곳에 모아 관리하기 위함. scratchpad는 경로가 깊어 접근이 불편하다.
단, 프로젝트 내부 로그/보고(`&하네스\logs\` 등)와 하네스 상태 파일(`phases/`, HANDOFF.md)은
이 규칙과 구분한다. 기존 폴더에 동일 산출물이 있으면 덮어쓰기 전에 대상을 확인하고 보고한다.

## MD 산출물 포맷 규칙
사용자에게 전달하는 MD 파일(바탕화면 MD·PR리뷰 등)의 **포맷 정본은 `&하네스\PR_작성규칙.md`의
8절(산출물 형태별 형식)·9절(MD 파일 포맷)·10절(PR리뷰 저장 구조)**이다.
(2026-07-21 이관 — 규칙이 CLAUDE.md와 PR_작성규칙.md에 나뉘어 표 형식 충돌·누락 사고가
발생해 한 파일로 통합함. 경위: 2026-07-20 PR리뷰 MD가 HTML 표만 쓰고 style 블록 누락)
MD 산출물을 만들기 전에 해당 절을 읽고 따른다. 요지: 상단 `<style>` 블록 + 마크다운 표 +
✅/❌ 아이콘 적극 사용, HTML 표는 GitHub PR 본문 전용, PR리뷰는 작업별 폴더에 MD+아티팩트 이중 산출.

## 인코딩 규칙 (Windows cp949 환경 — 위반 시 반복 장애)
이 환경은 Windows 한국어 로케일(ANSI 코드페이지 cp949)이다. 인코딩을 명시하지 않은 텍스트 입출력은 cp949로 처리되어, UTF-8 한글·특수문자(`—`, `✓`, 이모지)에서 UnicodeDecodeError/UnicodeEncodeError가 발생한다. (실제 사례: execute.py가 CLAUDE.md 읽기·git 출력 디코드·print에서 3회 연속 크래시)

- **Python**: `open()`, `Path.read_text()/write_text()`, `subprocess.run(text=True)`에 **항상 `encoding="utf-8"` 명시** (외부 프로세스 출력은 `errors="replace"` 권장). 새 스크립트는 시작부에 `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` (stderr 동일) 추가. 실행 시 `PYTHONUTF8=1` 병행 권장.
- **PowerShell 5.1**: 파일 쓰기 시 `-Encoding utf8` 명시 (기본값은 UTF-16/ANSI).
- **배치(.bat)**: 첫 부분에 `chcp 65001` 포함.
- **커밋 메시지·로그 포맷 문자열**: 비ASCII 특수문자(`—`, `✓`, `↻` 등) 사용 금지. ASCII(`-`, `OK`, `retry`)로 대체. 이유: cp949 콘솔로 출력될 수 있다.
- **파일 생성**: 소스·문서 파일은 UTF-8(BOM 없음)로 저장한다.

## 개발 프로세스
- 커밋 메시지는 conventional commits 형식을 따른다 (feat:, fix:, refactor:, chore:).
- PR 설명·PR 리뷰 문서를 작성할 때는 `&하네스\PR_작성규칙.md`를 참고해 그 구조(헤더 메타 줄, 필수 섹션 순서, `<details>`+`<summary>` 접힘, 전부 한글)를 따른다.
- 사실과 추론을 구분한다. 모르는 것은 모른다고 말하고, 추론이 필요한 경우 "추론:" 으로 명시한다.
- 추측을 사실처럼 단정하지 마라. 저장소·코드·동작에 대한 주장은 싸게 확인되는 것이면 말하기 전에 확인하고(파일 읽기·grep·빌드), 단정→정정 번복을 피한다. 확인 불가한 것은 가정·근거를 함께 제시한다.
- 로드된 컨텍스트(시작 시 주입된 규칙/메모리, 보여진 diff/발췌)는 stale하거나 일부일 수 있다. 규칙·기준에 근거해 판단/보고하기 전, 또는 'X modified' 신호를 받으면, 원본 파일을 Read로 전체 재대조한 뒤 진행한다.
- 코드를 변경한 뒤에는 반드시 검증한다: Java/설정 변경은 `gradlew build`로 컴파일 확인, 로직 변경은 관련 테스트(pytest)나 실화면(qa_agent) 확인. 검증 결과(통과/실패·미실행 여부)를 답변과 HANDOFF에 사실대로 기재한다.
- 변경 반영 방식: Java·매퍼 XML·yml 변경은 dev 서버 재기동이 필요하고, JSP/CSS/JS는 핫리로드로 즉시 반영된다. (JSP 핫리로드는 application-local.yml의 `server.servlet.jsp` 설정이 전제 — 2026-06-30 키 위치 교정으로 작동)

## 지적사항 대응: 근본원인 + 검증방법 회고 (필수)
사용자가 지적(결함/오류/누락)을 주면, 그 부분만 고치고 끝내지 마라. 지적이 왜
발생했는지 경위를 파악하고, 같은 현상이 재발하지 않도록 검증 자체를 교정한다.
지적 1건마다 아래를 거친다:

1. 검증 여부 확인: 그 항목에 대한 검증이 실제로 이뤄졌는가?
   - 검증을 했는데도 지적이 났다면 -> 검증 방법의 어떤 실수로 못 걸렀는지 규명한다.
     (예: 잘못된 조건/해상도/입력으로 테스트, 표면만 보고 실제 동작/렌더를 안 봄,
      기준과 대조하지 않고 추측, 생성물이 아닌 옛 스냅샷을 검증 등)
   - 검증을 안 했다면 -> 왜 누락했는지 파악한다(범위 가정 오류, 비용 회피, 단정 등).
2. 근본원인 교정: 지적 자체를 고치는 것에 더해, "왜 발생했는가"의 원인을 제거하고
   그 원인을 잡아낼 수 있도록 검증 방법을 보강한다(올바른 기준·조건·도구로 재검증).
3. 보고: 지적별로 (검증했나 / 안 했나) - (놓친 이유 또는 방법 결함) - (교정한 검증)
   을 사실대로 기재한다. 같은 류의 지적이 반복되면 검증 절차를 규칙으로 승격한다.

핵심: 검증은 "기준과 직접 대조해, 실제 사용 조건(예: 실제 해상도)에서, 생성물 자체를"
확인하는 것이다. 그때 그 '기준'이 최신인지부터 확인한다(시작 스냅샷 신뢰 금지).
단정과 표면 점검은 검증이 아니다.

- 가는 선/보더/1px 구분선/미세 색차는 **다운스케일된 비교 이미지에선 사라져 안 보인다.**
  반드시 ①원본 해상도(또는 확대) 작은 크롭으로 보고 ②computed style(border-width/color,
  background)까지 코드로 측정해 확인한다. 전체화면을 축소한 스크린샷만으로 "구분선 OK"라고
  판단하지 마라(반복 실패 사례).
- 코드(마크업·함수·쿼리)가 **존재한다는 사실만으로 "화면에 노출된다/동작한다"고 판정하지 마라.**
  정의 → 호출·라우팅 경로 → 트리거 생존(실제 이벤트 배선)의 3단계를 추적해 도달성을 확인하고,
  가능하면 실화면으로 확인한다(특수가공 노출 오판 반복 사례).

## 명령어
```bash
# 빌드 (컴파일 오류 확인)
cd E:/harness_framework/Projects/Tomes-Cloud && ./gradlew build -q

# dev 서버 기동 (인자 필수 — application.yml이 비어 있고 local 기본 포트=80이므로 8081 명시)
cd E:/harness_framework/Projects/Tomes-Cloud && ./gradlew bootRun --args='--spring.profiles.active=local --server.port=8081'

# 단위 테스트
cd E:/harness_framework/Projects/Test-Automize && pytest tests/ -q

# 실제 화면 QA 검증 (Step AC에서 사용)
cd E:/harness_framework/Projects/Test-Automize && python -m qa_agent.agent_run --row {TC_ROW_NUMBER} --skip-global-seeds
```

## 주요 경로 참조
PATHS.md 참조: E:\harness_framework\PATHS.md
