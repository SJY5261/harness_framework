# Codex 저장소 진입점

Codex가 이 저장소의 주 에이전트이자 기본 오케스트레이터다. 작업을 시작할 때 다음 정본을 순서대로 읽는다.

1. `PROJECT_RULES.md` — 공통 프로젝트 규칙과 개발·안전·검증 규칙
2. `HANDOFF.md` — 현재 작업 인덱스
3. 사용자 요청과 관련된 `handoff/<작업>.md` — 해당 작업의 상세 상태

과거 기록에 Claude 중심 절차가 남아 있어도 현재 운영 규칙으로 재해석하지 않는다. 충돌하면 사용자 지시, 이 파일, `PROJECT_RULES.md`, 관련 HANDOFF의 최신 결정 순으로 우선한다.

## 기본 작업 방식

- Codex가 조사, 구현, 검증, HANDOFF 갱신, 결과 보고를 끝까지 수행한다.
- Claude를 자동 호출하지 않는다. 사용자가 명시적으로 요청하고 Claude가 사용 가능할 때만 선택적 교차 검토에 사용한다.
- 상대 모델의 부재만으로 `peer_review_pending`을 만들지 않는다. 보안·데이터 손실·비가역 설계처럼 독립 관점이 실제로 결과를 바꿀 수 있는데 검토가 수행되지 않았을 때만 남긴다.
- 작업 범위를 벗어난 기존 미커밋·미추적 파일은 사용자 작업으로 보고 건드리거나 커밋하지 않는다.

## 진행 안내

- 3단계 이상이거나 3분 이상 걸릴 작업은 착수 전에 단계 목록과 현재 단계를 알린다.
- 단계 전환, 검증 시작, 블로커 발생, 5분 이상 새 안내가 없을 것으로 예상될 때 `완료한 일 / 현재 하는 일 / 다음 일`을 1~3문장으로 알린다.
- 실제 체크리스트나 전체 작업 수가 확정된 경우에만 진행률 숫자를 사용한다. 근거 없이 완료율이나 남은 시간을 추정하지 않는다.
- 도구 출력 원문을 진행 안내로 대신하지 않는다. 사용자가 판단할 수 있는 의미와 다음 행동을 요약한다.

## 계획·협업

- 독립적인 병렬 작업이 명확하고 사용자 또는 적용 가능한 지시가 위임을 요청한 경우에만 Codex 서브에이전트를 사용한다.
- 단순 구현과 명백한 수정은 Codex가 직접 수행하고 관련 테스트와 diff 검토로 완료한다.
- 설계 갈림길, 회귀 위험이 큰 변경, 보안·데이터 손실 가능성이 있는 변경은 `discussion/TEMPLATE.md`로 결정 기록을 만든다. 기본 모드는 Codex 단독 분석+반론 검토이며, Claude 참여는 선택 사항이다.
- 토론은 최대 3라운드로 제한하고 정확성, 저장소 근거, 구현·운영 비용, 회귀 위험, 검증 가능성을 판정 기준으로 사용한다.

## 검토와 완료 조건

- 저장소 사실은 원본 파일, `rg`, git diff/status, 빌드·테스트 결과로 확인한 뒤 보고한다.
- 코드 변경 후 `PROJECT_RULES.md`에 정의된 관련 검증을 수행한다.
- 최종 답변 전에 요청 충족 여부, 예상치 못한 diff, 테스트 결과, 미검증 범위를 자체 검토한다. 필요하면 Codex `/review` 또는 별도 리뷰 작업을 사용한다.
- Claude 전용 `answer-reviewer`, 답변 배너, Stop 훅은 Codex 완료 조건이 아니다.

## Codex 설정 경계

- 저장소 공용 Codex 설정과 훅은 `.codex/`에 둔다.
- 개인 모델, 요금제, 인증, 알림, 전역 권한은 저장소 설정으로 강제하지 않는다.
- 프로젝트 설정·훅 변경은 무엇을·왜 바꾸는지 설명하고 사용자 승인을 받은 뒤 수행한다.
