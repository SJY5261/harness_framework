# 단가검토 — 현재 인수인계

_최종 상태: 2026-08-03 단가입력 개편과 기록조회 모드 변경을 독립 stash로 보관, DB·복사 정책 사용자 결정 대기. 긴 진행 이력은 [역사 보존본](../logs/handoff/단가검토_2026-07-16_history.md)에 있다._

## 현재 상태

- 견적 단가입력 컴포넌트 개편은 스펙 변경을 반영해 실화면 15개 항목을 검증했다.
- 기록조회 화면은 조회 전용 모드, 선택 행 하이라이트, Tab 셀 이동 차단까지 검증했다.
- 관련 `%클라우드` 변경 3파일은 독립 stash `24c94b4db0dfafebf70f39d1bd2b0940bc8a376d`로 보관했다. 재개 시 stash 번호가 아니라 이 객체 ID와 현재 브랜치 diff를 확인한다.
- 정확한 구현·원복·검증 경위와 과거 화면 결정은 역사 보존본에 보존했다.

## 확정 결정

- 기존 비율형 6개 입력은 직접 금액 입력 중심의 새 스펙으로 전환했다.
- 기록조회 모드에서는 편집·저장 동작을 막고 조회·선택 동작만 허용한다.
- 일반 보고나 handoff의 과거 `answer-reviewer`, grilling 표기는 당시 이력이며 현재 Codex 실행 의존성이 아니다.
- Jira·첨부 이미지가 최신 화면 정본이면 직접 확보해 대조하고, 접근할 수 없으면 구현을 중단한다.

## 사용자 결정 대기

1. 배송비 저장 컬럼의 최종 이름과 타입
2. 견적 값을 수주로 복사할지 여부와 복사 시점
3. 위 결정에 따른 DDL 및 기록조회 후속 범위

## 재개 순서

1. 사용자 결정을 확인한다.
2. `%클라우드`의 현재 브랜치·dirty 상태와 관련 파일을 다시 측정한다.
3. 최신 Jira·첨부 이미지를 정본으로 직접 확보해 현재 화면과 비교한다.
4. 외부 자료를 확보하지 못하면 추정 구현하지 않고 사용자 검토를 요청한다.
5. DB 변경안은 실행 전 대상·DDL·데이터 보정·검증·복구 SQL을 제시한다.
6. 구현 후 제품 빌드와 관련 실화면 검증을 실행한다.
