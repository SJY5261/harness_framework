# Codex 저장소 운영 규칙

Codex가 이 저장소의 주 에이전트다. 이 파일은 매 작업에 자동 적용되는 짧고 안정적인 규칙만 담는다.

## 작업 시작

1. `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/pc_profile.ps1 -Json`으로 현재 PC 프로필을 확인한다. 저장소 경로·junction·호스트명으로 PC를 추정하지 않는다. `unknown`이면 PC별 경로에 쓰거나 환경을 변경하기 전에 사용자 확인을 받아 `-SetProfile work|personal`로 등록한다.
2. `PROJECT_RULES.md`에서 제품·안전·검증 규칙을 확인한다.
3. `HANDOFF.md`에서 사용자 요청과 일치하는 활성 작업을 찾는다.
4. 일치하는 작업이 있을 때만 해당 `handoff/<작업>.md`를 읽는다.
5. 경로가 필요하면 `PATHS.md`, 기록 체계가 필요하면 `docs/SECOND_BRAIN.md`를 읽는다.
6. PR 설명·PR리뷰·사용자 전달 보고서를 만들 때는 작업 전에 `PR_작성규칙.md`를 읽고 작업별 폴더의 MD 정본·자체완결 HTML 완료 조건을 따른다. Sites 게시는 사용자가 명시적으로 요청한 경우에만 수행한다.
7. Jira·MCP·첨부 이미지 등 외부 자료가 요구사항의 정본인데 확보하지 못하면 `docs/processes/EXTERNAL_REQUIREMENTS.md`에 따라 구현을 중단하고 사용자 검토를 요청한다.

과거 문서의 Claude 절차는 역사 기록이다. 충돌 시 사용자 지시 → 가까운 `AGENTS.md` → 루트 `AGENTS.md` → `PROJECT_RULES.md` → 관련 HANDOFF의 최신 결정 순으로 적용한다.

## 환경 맞춤 판단

- 모든 작업에서 일반론의 이론상 최고·최대 안전·최소 비용을 자동 정답으로 삼지 않는다. `context/판단기준.md`의 사용자 환경, 실제 발생 가능성, 현재 제약, 편익과 불편을 함께 비교해 이 사용자에게 과하지 않은 최적안을 선택한다.
- 확인된 현상이나 현실적인 위험이 없는 최적화·보완은 기본 작업에 포함하지 않는다. 필요한 조치, 현재 환경에서 유효한 권장안, 이론상 가능한 선택지를 구분하고 선택 사항은 선택 사항으로만 보고한다.
- 환경에 따라 판단이 달라지는 작업은 `context/README.md`에서 필요한 문서만 골라 읽고, 환경 폴더 전체를 자동으로 주입하지 않는다.
- PC별 경로와 로컬 상태는 `scripts/pc_profile.ps1`의 판정값과 `PATHS.md`의 프로필 매핑을 사용한다. 프로필 마커는 저장소 밖 `D:\ToolData\Harness`에 두며 Git에 기록하지 않는다.
- 작업 중 필요한 환경 사실을 시스템에서 안전하게 확인할 수 있으면 읽기 전용으로 조사해 `context/`의 해당 정본을 갱신한다. 시스템에서 확인할 수 없고 결과를 실질적으로 바꾸는 정보만 사용자에게 요청한다.
- 환경 기록에는 비밀번호·토큰·인증 원문·DB 업무 데이터·호스트명·계정명·일련번호·네트워크 식별자를 저장하지 않는다.

## 실행 원칙

- 요청을 조사·구현·검증·기록까지 끝낸다. 설명·진단 요청은 명시적 변경 요청이 없으면 수정하지 않는다.
- 먼저 `rg`와 git 상태로 범위를 좁히고, 관련 없는 대형 문서·로그·미추적 파일을 읽거나 변경하지 않는다.
- 현재 업무 PC에서 설치 위치를 선택할 수 있는 사용자 앱·개발 도구·런타임·CLI의 실행 파일은 `D:\Tool`, 확장·브라우저 런타임·사용자 설정·캐시·전용 임시 데이터는 `D:\ToolData`를 정본으로 삼는다. C: 사용자 설치나 실데이터를 발견하면 D: 중복 여부를 버전·기능·해시·실행으로 확인한 뒤, 중복은 C:만 제거하고 고유 구성은 D:로 이관·검증한 후 C: 원본을 제거한다. 기본 경로 호환이 필요하면 C:에는 D: 대상 junction이나 최소 포인터만 남긴다. 실행 중인 설치는 세션을 강제 종료하지 않고 안전한 재시작 시점까지 삭제를 연기한다. Windows·드라이버·제품 설치 관리자가 C: 위치를 고정하는 구성요소와 Windows 설치 캐시는 수동 이동으로 깨뜨리지 않고 예외를 보고한다.
- 기존 사용자 변경은 보존한다. 커밋할 때 이번 작업 파일만 명시적으로 스테이징한다.
- 코드 수정 시 요구사항 달성에 필수적인 줄만 변경하고 diff 최소화를 최우선한다. 필요한 코드 변경에 직접 수반되는 경우가 아니면 공백·들여쓰기·줄바꿈·EOF 개행·정렬·포맷팅 등 비기능적 정리 변경을 만들지 않는다.
- 불명확하지만 되돌릴 수 있는 세부사항은 합리적으로 가정하고 진행한다. 비가역 변경, 데이터 손실, 권한 확대, 요구 결과가 달라지는 선택만 사용자에게 확인한다.
- 작업 중 새 실패·경고·잠재 결함·회귀·보안 위험을 발견하면 현상·영향·채택한 안전 조치를 결과에 명시한다. 현재 범위 안에서 되돌릴 수 있고 안전한 해결책이 명확한 경우(예: 신규 의존성의 안전 버전 선택)는 별도 확인 없이 가장 안전한 방법을 적용한다. 데이터 손실, 권한·공개 범위 확대, 비가역 변경, 호환성이 크게 갈리는 선택, 요구 결과 변경처럼 정말 중요한 결정만 사용자에게 확인한다.
- 별도 지시가 없으면 Codex 단독으로 작업한다. Claude 작업은 Claude 세션에서 별도 운영하는 것이 원칙이며, Codex는 `.claude/`를 자동 호출·수정하지 않는다. 현재 프롬프트에서 사용자가 Claude 사용을 명시한 경우만 예외다.
- 기존 grilling 절차는 폐기하며 Codex용으로 이식하거나 자동 대체하지 않는다.
- 서브에이전트는 사용자나 더 구체적인 지시가 위임을 허용하고 독립 병렬 작업이 실제로 있을 때만 사용한다.

## 계획과 진행 안내

- 3단계 이상이거나 장시간 작업은 시작 시 단계와 완료 조건을 공유한다.
- 단계 전환, 검증 시작, 블로커 발생, 5분 이상 무안내가 예상될 때 진행 상황을 짧게 알린다.
- 진행률 숫자는 전체 작업 수가 확정된 경우에만 사용한다. 근거 없는 완료율·남은 시간을 추정하지 않는다.
- 도구 출력 대신 `완료 / 현재 / 다음`의 의미를 요약한다.

## 완료 조건

- 요청 충족 여부와 diff를 자체 검토하고 관련 테스트를 실행한다.
- 테스트를 실행하지 못했으면 이유와 남은 위험을 명시한다.
- 현상·문제·변경을 보고할 때는 `현상 → 원인 → 해결 → 채택 이유 → 결과` 순서를 명시한다. 해당 단계가 없는 단순 사실 보고는 억지로 채우지 않고 `해당 없음` 또는 생략 이유를 밝힌다.
- 원인과 결과는 확인된 사실과 추론을 구분하고, 해결 방법을 선택한 이유에는 대안 대비 장점·위험·비용 중 실제 판단 근거를 포함한다.
- 작업 상태가 바뀌었을 때만 관련 handoff와 `HANDOFF.md` 한 줄을 갱신한다. 단순 질의나 무변경 조사는 기록하지 않는다.
- `&하네스` 변경은 관련 파일만 커밋·푸시한다. `%클라우드` 커밋은 사용자가 명시적으로 요청한 경우에만 한다.
- 고위험 판단에 독립 검토가 실제로 필요하지만 수행하지 못한 경우에만 `peer_review_pending`을 남긴다.

## Codex 설정 경계

- 저장소 공용 설정·훅은 `.codex/`에 둔다.
- 개인 모델, 요금제, 인증, 알림, 전역 권한은 저장소에 고정하지 않는다.
- 반복되는 작업 절차는 `docs/processes/`의 조건부 절차, `templates/`, 스크립트·skill로 분리하고 이 파일에는 적용 조건과 강력한 공통 규칙만 둔다.
- 문서가 `docs/SECOND_BRAIN.md`의 크기·혼합 기준을 넘으면 원문을 보존한 채 현재 상태와 역사·양식으로 자동 분리하고 결과를 보고한다.
