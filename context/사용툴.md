# 사용 도구

_최종 시스템 확인: 2026-07-30 / 현재 업무 PC_

## 주 개발·운영 도구

| 도구 | 버전·경로 | 현재 용도·상태 |
|---|---|---|
| Codex CLI | `0.146.0` | D: 실행 정본 확인. 기본 에이전트이며 개인 설정은 `gpt-5.6-sol`, `xhigh`, pragmatic |
| Git | `2.49.0.windows.1`, `D:\Tool\Git` | 버전 관리 |
| GitHub CLI | `2.96.0`, `D:\Tool\GitHub CLI\bin\gh.exe` | 실행 파일·설정·credential helper D: 이관, keyring 인증 확인 |
| Node.js / npm | `24.17.0` / `11.13.0`, `D:\Tool\nodejs` | Codex CLI와 Node 도구 |
| Python | `3.14.6`, `D:\Tool\Python\Python314` | `python`·`py`와 모듈 실행은 D: 정상. 기존 패키지는 보존됐지만 콘솔 실행기 21개가 제거된 C: Python 경로를 내장해 직접 실행 복구가 필요 |
| Java | Oracle JDK `21.0.2`, `D:\Tool\jdk-21\jdk-21` | `%클라우드` 기본 JDK |
| Gradle Wrapper | `8.14.3`, `%클라우드` 저장소 래퍼 | 제품 빌드. 전역 Gradle은 사용하지 않음 |
| Apache Tomcat | `9.0.102`, `D:\Tool` | 로컬 웹 실행 자산 |
| MariaDB | `11.4.5`, `D:\Tool\mariaDB` | 로컬 DB·클라이언트 |
| DBeaver | `25.0.1`, `D:\Tool\dbeaver` | 실행 파일 D:. 사용자·Eclipse 데이터는 실행 종료 후 `D:\ToolData\DBeaver` 자동 이관 대기 |
| VS Code | `1.131.0`, `D:\Tool\Microsoft VS Code` | 본체 D:. 사용자 데이터·확장은 실행 종료 후 `D:\ToolData\VSCode` 자동 이관 대기 |
| Claude Code | `2.1.220`, `D:\Tool\Claude Code\bin\claude.exe` | D: 실파일과 사용자 `Path` 사용. 공식 고정 업데이트 경로인 C: `.local\bin`은 D: 대상 junction |
| IntelliJ IDEA | Ultimate `2024.3.5`, Community `2025.1` | Java IDE 설치 확인. 주 사용 에디션은 미확인 |
| SourceTree | `3.4.31` | Git GUI. 실행 종료 후 앱은 `D:\Tool\SourceTree`, 데이터는 `D:\ToolData\SourceTree`, Git은 `D:\Tool\Git`으로 자동 전환 대기 |
| pytest | `9.1.1` | `%테스트`와 하네스 Python 검증 |
| Playwright | `1.61.0` | 브라우저 자동화. Chromium 실실행 경로 `D:\ToolData\Playwright\Browsers` 확인 |

JDK 8과 17도 `D:\Tool`에 설치돼 있지만 현재 `%클라우드` 정본은 JDK 21이다. Docker, Maven, 전역 Gradle은 현재 셸 `Path`에서 발견되지 않았으며 현재 저장소 작업의 필수 조건도 아니다.

## Codex 운영 선호

- 사용량이 충분한 동안 토큰 절약만을 이유로 모델이나 추론 수준을 낮추지 않는다.
- 저장소 공용 규칙·훅은 `.codex/`, 개인 모델·권한·인증은 사용자 Codex 설정에서 관리한다.
- Claude는 사용자가 현재 프롬프트에서 명시했을 때만 Claude 세션에서 별도로 사용한다.
- Codex CLI의 사용자 `Path`와 새 셸 실행 정본은 `D:\Tool\nodejs`다. 불완전한 C: 0.145.0 설치는 D: 0.146.0 실행과 고유 데이터 부재를 검증한 뒤 제거했으며, `CODEX_HOME`의 `D:\ToolData\Codex` 전환은 개인 홈 이관 후 수행한다.
- 이관 전 환경을 상속한 VS Code에서도 새 터미널은 사용자 설정의 `terminal.integrated.env.windows`로 D: Node/Codex를 먼저 찾고, 기존 세션의 C: 호환 shim도 D: 래퍼를 호출한다.

## 작업 도구 데이터 정본

- 실행 파일: `D:\Tool`
- 확장·브라우저 런타임·설정·캐시·전용 임시 데이터: `D:\ToolData`
- 이관·C: 원본 제거 완료: Playwright, npm·Next.js·pip 데이터, Gradle 사용자 홈, Python user base, GitHub CLI config·local data, JetBrains·Unreal Engine 데이터 경로
- 후속 이관 대기: VS Code 사용자·profile·확장·shared storage, GitHub Copilot, Codex 개인 홈·임시 데이터, SourceTree 앱·local·roaming, DBeaver 사용자·Eclipse 데이터, Git global config
- C: 기본 경로를 강제하는 도구는 업데이트·기존 바로가기 호환을 위해 D: 대상 junction만 남긴다. Windows·드라이버·브라우저 본체·설치 관리자 캐시는 대상이 아니다.

## 프로젝트 기술 스택

제품 기술과 버전 불변식은 이 문서에 복제하지 않고 `PROJECT_RULES.md`를 정본으로 사용한다. 구체적인 실행 경로는 `PATHS.md`, 테스트 명령은 `PROJECT_RULES.md`와 관련 handoff를 따른다.

## 갱신 기준

- 버전·설치 경로·기본 실행 명령이 바뀌었을 때
- 새 도구가 실제 반복 작업에 사용되기 시작했을 때
- 설치 여부가 아니라 현재 `Path`와 실제 실행 결과를 확인했을 때

설치 프로그램 목록 전체를 수집하지 않고 현재 개발·검증 흐름에 실제로 필요한 도구만 유지한다.
