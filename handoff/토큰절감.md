# 토큰절감 (2026-07-20 시작)

## 작업 목표
7/20 오전 토큰 급소진(오전 한도 대부분) 원인 분석 후 재발 방지 개선.

## 원인 분석 결과 (실측, 환산=API 단가 가중치 기준)
- worklog 백필 15연속(10:07~10:30, 노션 정리 지시로 실행): 오전의 약 32% — 날짜당 세션 로그 ~10만 자를 콜드 세션으로 전액 지불.
- 오전 병렬 작업 세션 4개(08:30~10:06): 약 52% — 팽창한 컨텍스트로 88~119회 호출 반복(캐시읽기 각 1,000만+). 4세션 모두 서브에이전트 위임 0회(토큰 절약 규칙 미준수 실측).
- 세션 고정비: 새 세션 시작 컨텍스트 약 4.7만 토큰(시스템 2.6만 + CLAUDE.md 23.4KB 등 2만).

## 완료
- `scripts/daily_worklog.py` 절감 개선 (검증: py_compile OK + --no-claude dry-run + sonnet 실호출 dry-run 모두 통과):
  - `--model` 옵션 신설, 기본 **sonnet** (기존: CLI 기본 모델 = 최상위).
  - 메시지당 2,000자 절단(MSG_CHAR_CAP), 세션 자료 상한 200k→120k자(SESSION_CHAR_CAP).
  - 효과 실측: 7/16 세션 자료 10만→2.3만 자.

## 완료 (추가, 2026-07-20 오후)
- 노션 백필 캐치업 완료: 7/17(금)은 3개 저장소 커밋 0건·세션 jsonl 0건으로 실제 무활동 확인(백필 불필요). 6/22~7/16 전 영업일은 이미 실전송 완료 상태였음. 7/20(오늘) 몫을 `daily_worklog.py --date 2026-07-20`(model=sonnet, 실전송)로 실행해 rc=0 확인 — 트래커 행 신규 2건(t-0068 토큰급소진분석=완료, t-0069 마켓플레이스kit설치=진행중) + 기존 4건 진행분 추가. 원장 갱신 확인. 노션 트래커는 오늘자까지 실데이터 기준 최신.

## 완료 (복구, 2026-08-04)
- 실제 Notion 작업 트래커의 마지막 수정 시각 `2026-07-22 17:33:36 KST` 이후 업무 기록을 날짜별로 다시 선별했다. 7/23·7/24·7/27~7/31·8/1~8/4 기록을 신규 작업 14개와 기존 작업 진행분 28건으로 반영했고, 신규 14개와 자동화 작업 페이지를 API로 다시 읽어 속성·본문·컬렉션 연결을 확인했다.
- 다른 작성자의 커밋만 근거로 생성된 작업 6건과 기록 금지가 명시된 사적 대화는 제외했다. 기존 데이터베이스 속성과 `daily_worklog.py` 본문 블록 구성이 충분해 템플릿·기존 행의 구조는 변경하지 않았다.
- 중단 원인은 두 단계였다. 7/27~7/28에는 `NOTION_TOKEN` 부재로 로컬 JSON만 생성됐고, 7/29부터는 `scripts/run_worklog.bat`이 존재하지 않는 C: Python을 호출했다. 배치가 Python의 실패 코드를 전달하지 않아 예약 작업 결과가 성공으로 오인되기도 했다.
- `scripts/run_worklog.bat`의 Python을 `D:\Tool\Python\Python314\python.exe`로 전환하고 실행 파일 누락과 Python 종료 코드를 호출자에게 전달하도록 수정했다. 실제 배치 dry-run에서 D: 런타임과 종료 코드 0을 확인했다.

## 진행중·미완
- CLAUDE.md 다이어트(저빈도 섹션 분리로 세션 고정비 인하) — 분리안 제시 후 사용자 승인 대기.
- 예약 작업 `DailyWorklog`은 활성·Ready 상태지만 공식 Notion Integration 토큰과 데이터베이스 ID가 프로세스·사용자·시스템 환경에 없다. 현재 배치는 로컬 JSON 생성까지만 가능하므로 안전한 자격 증명 공급 복구와 다음 예약 실행의 실제 전송 검증이 필요하다.
- 2026-08-04 17:41 오늘 최종 작업 4건을 `logs/worklog_2026-08-04.json`으로 다시 생성했다. 재질·표면처리 단일 PK 전환과 테넌트별 개별 D03 비활성화 설계 검토까지 포함했으며, `logs/notion_이관대기.md`에 미전송 상태로 보존했다. 사용자 계획에 따라 2026-08-05 인증 복구 후 `--from-json`으로 전송한다.

## 블로커·주의
- `logs/worklog_2026-07-16.json` 로컬 요약 파일은 아침 검증 dry-run이 최소본으로 덮어쓴 상태로 남아있음(재생성 안 함 — 실데이터는 Notion 트래커·원장에 이미 반영돼 있어 영향 없음, 로컬 아카이브 파일 하나만 최소본).
- 2026-07-20 당시의 Disabled 기록과 달리 2026-08-04 재확인 시 `DailyWorklog`은 활성·Ready 상태다. 안정적 인증이 복구되기 전에는 성공 코드만으로 Notion 전송 완료를 판단하지 말고 로그의 `Notion send skipped` 여부를 함께 확인한다.

## 관련 파일·경로
- scripts/daily_worklog.py / logs/worklog_backfill_2026-07-20.log / logs/worklog_2026-07-20.json(dry-run 산출)
