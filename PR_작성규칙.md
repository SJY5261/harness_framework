# PR·리뷰·보고서 작성 규칙

이 문서는 PR 설명, PR 리뷰, 사용자가 파일로 요청한 보고서를 만들 때만 읽는 조건부 절차의 정본이다.
공통 운영 원칙은 `AGENTS.md`와 `PROJECT_RULES.md`를 우선하며, 형식 예시는 아래 템플릿에서 가져온다.

- GitHub PR 설명: `templates/pr-description.html`
- 바탕화면 PR 리뷰: `templates/pr-review.md`
- 일반 보고서: `templates/general-report.md`
- 사용자 전달용 자체완결 HTML: `templates/standalone-report.html`
- DB 변경이 포함된 보고서: `docs/processes/DB_CHANGE_REPORT.md`

## 1. 공통 강제 규칙

1. 확인된 사실과 추론을 구분하고 모든 설명은 한글로 작성한다.
2. 필요한 섹션에 쓸 내용이 없으면 비우지 말고 `해당 없음`이라고 적는다.
3. 긴 설명은 `<details><summary>제목</summary> … </details>`로 접는다. `<summary>` 없는 `<details>`는 금지한다.
   `<summary>`에는 내용을 식별할 제목과 `(펼치기)`를 표시한다.
4. 검증 결과와 실행하지 못한 검증, 남은 위험을 구분한다.
5. 사용자 전달용 파일은 MD를 정본으로 삼고, HTML은 같은 MD에서 생성한다.
6. 비밀번호·접속 정보·실고객 데이터 등 민감정보를 산출물에 넣지 않는다.

## 2. GitHub PR 설명

### 헤더

제목 아래 한 줄에 다음 네 항목을 모두 둔다.

| 항목 | 내용 |
|---|---|
| 브랜치 방향 | 머지 원본 → 대상 |
| 대표 커밋 | 짧은 커밋 해시 |
| 변경량 | 파일 수와 추가·삭제 라인 |
| 상태 | WIP, 목업, 실연동 완료 등 현재 단계 |

### 섹션 순서

| 순서 | 섹션 | 적용 |
|---|---|---|
| 1 | 개요 | 필수 |
| 2 | 주요 변경 | 필수 |
| 2-1 | DB 변경 SQL | DB 변경 시 필수 |
| 3 | 설계 원칙 | 필수 |
| 4 | 동작 방식 | 필요한 경우 |
| 5 | 검증 완료 & 테스트 방법 | 필수 |
| 6 | 후속 계획 | 필요한 경우 |
| 7 | 알려진 사항 / 주의 | 필요한 경우 |
| 8 | 복구 지점 | 필수 |

- `개요`에는 무엇을 왜 바꾸는지와 현재 단계를 적는다.
- `주요 변경`은 파일·구분(신규/수정/삭제)·내용 표로 작성한다.
- `설계 원칙`에는 선택한 구조의 이유와 주요 전제를 적는다.
- `검증 완료 & 테스트 방법`에는 직접 확인한 결과와 리뷰어가 재현할 순서를 나눈다.
  특정 진입 경로나 선행 조건이 필요하면 인용문으로 먼저 강조한다.
- `복구 지점`에는 문제 발생 시 실제로 되돌릴 대상과 방법을 적는다.
- `동작 방식`, 긴 검증 절차, 긴 주의 사항, DB SQL은 `<details>`로 접는다.

GitHub에서는 표와 접힘을 HTML로 작성한다. 장식용 이모지는 쓰지 않고 완료·금지처럼 의미가 있는 신호만 최소한으로 사용한다.

## 3. 바탕화면 PR 리뷰와 일반 보고서

### PR 리뷰

PR 리뷰는 2절의 헤더 메타와 섹션 순서를 따른다. 표는 HTML이 아니라 일반 마크다운 표로 작성하고, 파일 상단에 `templates/pr-review.md`의 `<style>` 블록을 둔다.

### 일반 보고서

일반 보고서에는 PR 전용 브랜치·커밋·변경량을 강제하지 않는다. 내용은 다음 순서를 기본으로 한다.

1. 현상
2. 원인
3. 해결
4. 채택 이유
5. 결과

확인된 원인과 추론을 명시적으로 구분한다. 해당 단계가 없는 단순 사실 보고는 억지로 채우지 않고 생략 이유나 `해당 없음`을 밝힌다.

바탕화면 MD는 VS Code 미리보기를 기준으로 한다. 마크다운 표와 템플릿의 테마 대응형 `<style>`을 사용한다. 항목·비교·범위는 표로, 서술은 짧은 목록이나 문단으로 작성하고 구분에 도움이 되는 아이콘을 적극 사용한다.

### 자체완결 HTML

HTML은 MD의 내용과 문서 구조를 유지하고 `templates/standalone-report.html`의 문서 껍데기와 스타일을 사용한다.

- 본문·목록·표 셀은 단어 중간에서 강제로 줄바꿈하지 않는다. 현재 줄에 단어 전체가 들어가지 않으면 단어 앞에서 다음 줄로 넘긴다.
- 표 헤더와 `구분`·`상태`·`종류` 같은 짧은 값은 한 글자씩 세로로 쪼개지지 않아야 한다. 폭이 부족하면 셀을 찌그러뜨리지 않고 표 영역을 가로 스크롤한다.
- 순백색 배경과 순검정 글자의 강한 대비를 피하고, 저채도 배경·본문·강조색을 사용하되 본문과 표의 구분은 선명하게 유지한다.
- 긴 코드 블록은 내용을 강제로 꺾지 않고 코드 영역에서 가로 스크롤한다.
- 외부 CSS·폰트·스크립트 없이 열려야 하므로 필요한 스타일은 HTML 안에 포함한다.

## 4. DB 변경 조건

컬럼·인덱스·제약·프로시저·함수·트리거를 변경했다면 `docs/processes/DB_CHANGE_REPORT.md`를 추가 적용한다. 최소한 다음 항목이 있어야 한다.

- 대상과 적용 환경
- 변경 전·후 정의
- 백업부터 사후 검증까지의 실행 순서
- 실제 DDL/DML 또는 원문 미보존 사실과 재구성 근거
- 검증 쿼리와 결과
- 복구 SQL과 데이터 손실·호환성 주의

## 5. 저장과 게시

별도 경로 지시가 없으면 먼저 다음 명령으로 PC 프로필을 확인하고 반환된 `artifact_root` 아래 작업별 폴더에 정확히 두 파일을 둔다. 프로필별 경로 매핑의 정본은 `PATHS.md`다. 요약에 `/`가 있으면 `.`로 바꾼다.

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/pc_profile.ps1 -Json
```

```text
{artifact_root}\[MM.DD 요약]\
├── [MM.DD 요약].md
└── [MM.DD 요약].html
```

- MD가 정본이며 HTML은 외부 자산 없이 열리는 자체완결 파일이어야 한다.
- 에이전트 종류를 이유로 MD 또는 HTML을 생략하지 않는다.
- 수정은 MD에 먼저 반영하고 같은 내용으로 HTML을 다시 생성한다.
- `아티팩트.html`, `아티팩트.url` 같은 추가 파일을 만들지 않는다.
- 사용자가 다른 기준 경로를 지정하면 그 경로를 우선하되 작업별 폴더와 MD·HTML 두 파일 구조는 유지한다.
- 프로필이 `unknown`이거나 마커가 손상됐으면 과거 경로나 junction으로 추정하지 않고 사용자에게 PC 종류를 확인한 뒤 프로필을 등록한다.
- 2026-07-20 이전의 폴더 없는 단일 MD는 사용자가 요청할 때만 소급 변환한다.
- Sites 게시는 사용자가 명시적으로 요청한 경우에만 한다. 게시할 때 `.openai/hosting.json`의 `project_id`를 보존하고 사이트를 중복 생성하지 않는다.
- 게시 URL은 결과 보고에서 사용자에게 전달한다.
- Sites 기본 접근은 현재 사용자만 가능한 owner-only다. 공유·공개 전환은 대상과 범위를 확인받는다.

## 6. 완료 전 확인

- [ ] 산출물 유형에 맞는 템플릿을 사용했다.
- [ ] 필수 섹션과 `해당 없음` 표기를 확인했다.
- [ ] 변경 파일·검증 결과·복구 방법이 실제 diff와 일치한다.
- [ ] DB 변경 시 DB 전용 체크리스트를 적용했다.
- [ ] MD와 HTML의 내용이 같고 HTML이 자체완결형이다.
- [ ] HTML에서 단어가 중간에 잘리거나 표의 짧은 값이 세로로 쪼개지지 않는다.
- [ ] HTML이 공통 저피로 색상과 가로 스크롤 가능한 표 스타일을 사용한다.
- [ ] 작업 폴더에는 정해진 MD·HTML 두 파일만 있다.
- [ ] Sites를 요청받지 않았다면 게시하지 않았다.
