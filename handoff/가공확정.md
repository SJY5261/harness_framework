# 가공확정 비활성화 (KAN-63)

## 2026-08-12 현재 구현 상태

- 구현 위치는 `%클라우드` 저장소 `D:\harness_framework\Projects\Tomes-Cloud`, 브랜치는 `sunPro/kan-63-remove-process-confirm`이다. V1 XML 보정 3파일까지 포함하도록 기존 원격 커밋을 amend하고 `fde3d05`로 force-with-lease push했다. 원격과 로컬 HEAD는 일치하며 worktree는 만들지 않았다.
- 가공확정 화면·controller·API·기존 데이터는 보존하고 정상 메뉴에서만 숨긴다. 작업지시 확정·취소의 기존 `S53R14/S53R16` 생성도 유지하며, 동시에 생성되는 `S11R10/S11R20`을 후속 기능의 판단 기준으로 사용한다.
- 자재, POP, 도면, MCT, 검사에서 R14/R16 판단 조건만 S11 기준으로 교체했다. S53 생성 중단이나 `PART_STATUS=NULL` 허용을 위한 변경은 최종 범위에서 제외했다.
- 구버전/V1 쿼리 쌍을 점검해 활성 자재 팝업 4종을 V1으로 전환하고 V1 파라미터 형식에 맞췄다. 대응 V1이 없는 활성 작업계획·바코드·팝업·서비스 검증 쿼리는 구버전 XML을 수정했다.
- 개발 DB의 `TBL_CODE`·`TBL_BASIC_CODE`에서 `S53R14/S53R16` 각 2행, 총 4행을 `DEL_YN='Y'`로 커밋했다. 원본은 `KAN63_BAK_TBL_CODE_20260812`, `KAN63_BAK_TBL_BASIC_CODE_20260812`에 보존했으며 현재값·진행이력 데이터는 변경하지 않았다.
- 최종 작업트리에서 전체 Gradle 빌드, JavaScript 구문, 변경 XML 8개 파싱, `git diff origin/main --check`, 호출·파라미터 계약 검증을 통과했다. 최신 서버와 개발 DB를 사용한 최종 실동작 검증은 미실행이다.
- PR 보조 결과서는 `C:\Users\User\Desktop\PR\[08.12 KAN-63 PR 검증 결과]\`의 동명 MD·자체완결 HTML 두 파일이다. 코드 변경 요약은 사용자 요청에 따라 제외했다.

## 작업 목표
지라 KAN-63 "[작업관리] 가공확정 삭제". 가공확정 화면과 기존 데이터는 보존한 채 메뉴와
`S53R14(가공확정)`·`S53R16(가공확정취소)` 기준코드를 비활성화한다. 작업지시 확정·취소 시 기존 S53 상태 생성은 유지하되, 후속 기능의 판단 기준은 동시에 저장되는 `S11R10/S11R20`으로 전환한다.

## 확정된 전제·결정
- **2026-08-12 사용자 최종 결정:** 화면·controller·API·기존 R14/R16 현재값과 이력은 삭제하거나 초기화하지 않는다. 메뉴와 기준코드만 `DEL_YN='Y'`로 숨기고, 작업지시 확정·취소의 기존 R14/R16 생성은 유지한다. 후속 기능의 판단 기준만 S11으로 전환한다.
- `/processControlConfirmProcess`를 포함한 비활성 화면 소스는 재활성화 가능하도록 보존한다. 정상 메뉴 흐름에서는 해당 화면이 노출되지 않는다.
- 기존 데이터 표시와 과거 이력 호환을 위한 R14/R16 참조는 유지한다. 신규 NULL 흐름을 실제로 차단하는 조건만 작업지시 확정 또는 후속 실제 상태 기준으로 변경한다.
- KAN-6 화면 범위는 사이트 공통 데스크톱 `bottom.jsp`로 한정한다. 배지는 아이콘 수급 전까지, 다른 디바이스 작업상세정보는 공통 화면 완료 후 별도 작업으로 보류한다.
- 테스트 업무 PK·회사·소재·외주업체는 실행 시점의 활성 데이터를 동적으로 조회한다. 고정값은 격리 검색용 `QA-NOR14-` 식별자와 분기 정의 코드뿐이다.
- Test-Automize 규칙 오해 해소: "테스트 깨지 말라"는 무관 변경·케이스 임의수정 금지이지, 의도된 프로세스 변경에 테스트가 따라 바뀌는 것까지 막는 게 아님. 테스트 갱신은 걸림돌 아닌 후속 작업.
- 지라 KAN-63 하위 3건: KAN-6(작업상세정보 가공확정 일시 제거·진행상태 재배치), KAN-35(작업지시관리에서 가공확정·제조원가분석 Hidden, 필수값 체크는 작업지시확정 시), KAN-38(신규 '작업지시 현황추적' 화면 — 현 코드+origin/feat/work-manage-ui 둘 다 미존재). 과거 KAN-6 문구의 “플래그 유지”보다 2026-08-11 사용자의 R14/R16 미사용 결정이 우선한다.

## 흐름 안전성 결론 (검증 완료)
변경 전 기준선은 R14/R16 없이 7개 분기 중 외주 1개만 통과했다. 상태 생성과 핵심 게이트를 작업지시 확정 기준으로 교체한 뒤 같은 7개 분기와 안전·가시성 3개를 실제 개발 서버에서 실행해 **10/10 PASS**를 확인했다.
- 확정·취소·재확정: 작업지시 `S11R10 → S11R20 → S11R10`, 파트 상태 `NULL`, R14/R16 미생성.
- 구매소재: `S53R18 → 취소 NULL/S54R30 → 재주문 S53R18 → POP S53R20` 정상.
- 사급/재고 첫 POP: 각각 `S53R22/S53R24`, 내부 직행 POP·도면·MCT와 R14 이력 없는 가공중 도면·검사 정상.
- 외주는 변경 전후 모두 정상이며 회귀가 없다.
- 전용 데이터 7건의 R14/R16 현재값·진행 이력은 최종 0건이다.
- v1·TV·리포트·폐지 레거시 19개 경로를 추가 실행해 18 PASS/0 FAIL/1 BLOCKED를 확인했다. 차단 1건은 v1 도면보드의 E07 POP 위치→공장영역 매핑이 개발 DB에 없는 환경 문제다.

## 현행 결함 정리 (2026-07-22 검증)
가공확정/S53R14 이웃 조사 범위에서 확인된 현행 이상은 아래 둘. 둘 다 가공확정 폐지와 무관한 선재(先在) 결함.

### 결함 1 — 소재주문취소 이력 누락 (Q4, 파급=이력테이블 한정)
- MaterialServiceImpl.itemOrderHistoryOrderCancel(410-443): 소재주문 취소 시 material.updateItemOrderRegisterControlPartCancel(material.xml:887-894)로 TBL_CONTROL_PART.PART_STATUS만 'S53R14' 복원하고, TBL_CONTROL_PART_PROGRESS 이력 INSERT를 빠뜨림. `MaterialServiceImpl.java:440`에 TODO 주석 실재.
- 파급 확정: **화면 오표시 없음** — 진행상태명 함수 smd.SF_GET_PART_STATUS_NM(DB 저장함수, 소스엔 정의 없고 호출만 57곳/15파일)이 이력이 아니라 TBL_CONTROL_PART.PART_STATUS(현재값)를 읽음(DB SHOW CREATE FUNCTION으로 확인). **게이트 무해** — S53R14 이력 존재만 봄. 따라서 **파급은 이력테이블 완전성에만 국한**(이력 마지막 줄이 S53R18로 남음). 이력 기반 신규 기능 붙일 때만 표면화.
- 7/21 세운 수정계획(5스텝, 미착수): 신규 이력 INSERT 쿼리 insertItemOrderRegisterControlPartCancelProgress 추가(기존 queryId 불변, 롤백 UPDATE와 동일 WHERE=실제 바뀐 행만, 배치/NEXTVAL 대응) -> :438 직후 배선 -> 착수 전 확인 -> build+시나리오 -> dev 재기동(커밋 사용자 몫).

### 결함 2 — 소재입고(보/사) POP 자동기록 dead code (신규 발견)
- PopServiceImpl.createScanningBarcodePop: :109가 `controlPartInfo.get("S53R24")`(변수 createS53R24)로 읽는데, 이 맵을 채우는 활성 쿼리 popMapper.selectInsertPopPartStatus(pop.xml:172)의 해당 CASE 컬럼 **별칭이 `S50R24`**(pop.xml:179-182, 반환값도 'S50R24'/'S50R22'). 키 불일치로 createS53R24는 항상 null -> :177 `if(createS53R24 != null...)` 항상 false -> :179-186 progress INSERT/UPDATE 미실행.
- 결과: "가공확정(S53R14) 이후 POP 스캔 시 소재입고(보/사) 서브상태 자동추가" 기능이 **동작 안 함**(dead). 대조: 같은 메서드 S53R20 블록(:103)은 별칭도 S53R20이라 정상.
- 코드셋 분열(수정 시 주의): S50R2x를 읽는 곳은 tomes.xml:217(소재입고일)뿐인데 이 쿼리(selectPortalOrderDetailList/tomesMapper)는 java/jsp/js 참조가 안 잡혀 **죽은 코드**. 라이브 리더는 전부 S53R2x: v1/material.xml(item-order-register.jsp:148), v1/main.xml:114(main.jsp:140), process.xml(56-72 등), material.xml(87-104), main.xml:253, machine.xml(1204-1358), process_dashBoard.jsp:689(IN_MATERIAL 필터 "S53R20","S53R24","S53R22").
- **정본은 S53R2x**(tomes.xml:207 SF_GET_ETC_CD('S53',C.PART_STATUS)로 PART_STATUS 도메인이 S53 그룹에 묶임). 'S50R24'/'S50R22'는 PART_STATUS로 오인 재사용된 명명 혼동(pop.xml + 죽은 tomes.xml 두 곳). 정정: "S50 그룹=PART_STATUS와 무관"이라 단정하면 과장(tomes.xml:217이 실제로 S50R24/R22를 PART_STATUS로 취급).
- **올바른 수정 방향**: 단순히 Java 키를 S50R24로 맞추면 안 됨. POP가 S50R2x를 기록하면 라이브 S53R2x 리더가 못 잡는 활성 교차 불일치로 전환됨. 별칭·기록값을 정본 S53R24/S53R22로 통일해야 함.
- 참고: 현재 소재입고로 PART_STATUS에 실제 기록되는 값은 S53R20뿐(MaterialServiceImpl.java:308-314,350-356 -> material.xml:1343,1381 SET PART_STATUS='S53R20'). S53R24/S53R22/S50R24/S50R22를 SET/INSERT하는 코드는 저장소에 없음.

## 다음 단계
- [x] 결함 2의 활성 POP 별칭·기록값을 정본 `S53R24/S53R22`로 통일하고 실동작으로 확인했다.
- [x] 결함 1의 소재주문 취소 R14 복원 경로는 NULL 변경이 기능상 필수가 아니므로 기존 동작을 유지한다. R14 기준코드는 비활성이어서 상태명이 비어 보일 수 있지만 후속 프로세스는 S11R10 기준으로 진행된다.
- [x] v1·TV·레거시/보고서·백업 참조를 이관해 활성 소스의 R14/R16 직접 참조를 0건으로 만들었다.
- [x] 현재 DB의 R14/R16 현재값·이력은 그대로 보존하고, 공유 기준코드 4행만 백업 후 `DEL_YN='Y'`로 비활성화해 트랜잭션·커밋 후 조회까지 검증했다.
- [ ] v1 도면보드 행 검증을 위해 실제 E07 POP 위치→공장영역 매핑을 받거나 임시 격리 매핑 생성·정리 승인을 받는다.
- [x] KAN-35 제조원가 분석 메뉴 8행 영구 숨김: `TBL_MENU_BAK_KAN35_20260728`에 대상 8행을 백업한 뒤 트랜잭션 UPDATE·검증 SELECT·사용자 승인 COMMIT 완료.
- [ ] KAN-38 작업지시 현황추적 화면은 정확한 화면 사양이 확정될 때까지 구현 보류. 현재 저장소·원격 브랜치·DB 메뉴에는 구현 근거가 없으며, 확정 화면과 Jira 상세가 제공되면 재개한다.
- [ ] POP 바코드 스캐너 실기기 검증은 2026-07-29 사용자 결정으로 보류. 가공확정 완료의 블로커로 보지 않으며 별도 재개 요청이 있을 때 수행한다.

## 2026-07-28 Jira 구현 진행
- `stash@{0}`의 작업지시 재확정 예외를 현재 `88bf7dc` 작업트리의 `create_control.jsp`, `control-manage.js`에 복원했다. stash 기준 두 파일은 HEAD 이후 변경이 없어 동일 패치를 충돌 없이 반영했다.
- KAN-6: 작업상세정보 변형 5종(`bottom.jsp`, `pop_bottom.jsp`, `tv_mct.jsp`, `outgoing_manage.jsp`, `quality_dashBoard.jsp`)에서 가공확정 일시와 진행상태 표시 및 해당 바인딩을 제거했다. 상태값·조회 필드·후속 게이트 플래그는 유지했다.
- KAN-35: DB 실측 결과 수주관리/작업관리의 가공확정 메뉴는 전 시스템에서 이미 `DEL_YN='Y'`. 제조원가 분석(`MENU_SEQ=138`, `order/cost-analysis`) BASIC·CSPARK·CSTEST·HR·NIDSOFT·SMD·TEST·TS 8행을 `TBL_MENU_BAK_KAN35_20260728`에 영구 백업한 뒤, 사용자 승인에 따라 트랜잭션에서 `DEL_YN='Y'`로 변경하고 COMMIT했다. 커밋 전 대상·백업·갱신 각 8행과 잔여 노출 0행을 확인했고, 새 연결의 커밋 후 조회에서도 백업 8행·숨김 8행·노출 0행을 확인했다.
- 작업지시확정 시 필수값 검사는 `create_control.jsp`와 `control-manage.js`의 `#CONFIRMATION` 경로에 이미 배선돼 있어 중복 구현하지 않았다.
- KAN-38: 연결된 Jira 도구가 없고 공개 검색·로컬 산출물·현재 원격 브랜치·DB 메뉴에서 상세 사양을 찾지 못했다. 2026-07-28 사용자 결정에 따라 정확한 화면이 확정될 때까지 구현 자체를 보류하며, 제목만으로 필드·검색조건·권한·메뉴를 만들지 않는다.
- 검증: 관련 JS 2개 `node --check`, `git diff --check`, `gradlew build -q --max-workers=4 --no-daemon` 통과. KAN-35는 별도 연결의 커밋 후 SELECT까지 완료했다. KAN-38 실화면 검증은 구현 재개 후 수행한다.

## 2026-08-03 Git 작업 분리

- KAN-63 작업지시 재확정 예외 2파일은 독립 stash `ca1524ddac4f19964336418d77f0344ee62a6ec4`로 보관했다.
- KAN-6 작업상세 가공확정 일시·진행상태 제거 5파일은 독립 stash `942c7f7ad80b161f5609fa34f24d4f9f1d7ff1aa`로 보관했다.
- 기존 KAN-63 중복 stash `7c0923e84c79bcbf4ee312c4483ddb351c20e6d7`는 새 stash와 같은 2파일·동일 변경 라인 해시 `5090bcf0ac50a4b3705494c725a5790af372656a`임을 확인한 뒤 사용자 지시에 따라 삭제했다.
- 재개할 때는 stash 번호가 아니라 위 커밋형 객체 ID로 적용 대상을 식별한다.

## 2026-08-11 업무용 PC 작업트리 복원

- 업무 PC `%클라우드`의 `feature-control-0713`(`2aad3db`)에 KAN-63 stash `ca1524ddac4f19964336418d77f0344ee62a6ec4`와 KAN-6 stash `942c7f7ad80b161f5609fa34f24d4f9f1d7ff1aa`를 순서대로 적용했다.
- 두 변경은 충돌 없이 7파일의 unstaged 변경으로 복원됐고 `git diff --check`를 통과했다. 두 stash 원본은 안전 복사본으로 유지했으며, 기존 미추적 `.claude/`는 건드리지 않았다.

## 2026-08-11 가공확정 통합 stash 분리

- 사용자 요청에 따라 업무 PC `feature-control-0713` 작업트리의 KAN-63 2파일과 KAN-6 5파일만 통합 stash로 분리했다.
- 새 stash 객체는 `d2c8a638e1f508bd4672b47fc191245061c2c02b`이며 7파일 `+36/-27`이다. 테넌트 기준코드 캐시 작업과 `.claude/` 로컬 설정은 포함하지 않았다.
- 분리 후 가공확정 7파일은 작업트리에서 제거됐고, 기존 KAN-63·KAN-6 독립 stash도 그대로 유지했다.

## 2026-08-11 현재 작업 보관 후 가공확정 재적용

- 사용자 요청에 따라 `feature-control-0713`의 테넌트 기준코드 tracked 8파일과 미추적 SQL·`.claude/` 설정을 stash 객체 `a487f0c9f3b2def3ce44abf8ec62e9e8801ac305`로 전체 보관했다.
- 통합 가공확정 stash `d2c8a638e1f508bd4672b47fc191245061c2c02b`를 현재 작업트리에 다시 적용했다. 결과는 unstaged 7파일 `+36/-27`이며 원본 stash와 patch-id `9becd89e7858f886c3633f817bd16cefddfde418`가 일치하고 `git diff --check`를 통과했다.

## 2026-08-11 Jira 범위 재확인과 KAN-6 재구현

- 사용자가 KAN-63 정본을 "작업관리 > 가공확정 삭제, 가공확정 화면을 작업지시 현황추적으로 개편"으로 다시 확인했다. 작업지시 재확정 예외는 Jira 요구가 아니라 과거 영향 분석의 추론성 보완이었으므로 `create_control.jsp`, `control-manage.js` 변경을 원복했다.
- KAN-6 대상은 사이트 공통 작업상세정보의 `attr/tabs/bottom.jsp` 데스크톱 표로 확정했다. 기존 stash가 수정했던 품질·POP·출고·TV 화면과 `bottom.js` 변경은 모두 원복했다.
- `bottom.jsp`에서 가공확정 일시와 빈 셀을 제거했다. 재고번호 아래 왼쪽은 `작업확정 일시 → 소재주문 일시 → 소재입고 일시 → 진행상태`, 외주요청 일시 아래 오른쪽은 `외주입고 일시 → 가공시작 일시 → 가공완료 일시 → 출고완료 일시` 순서로 재배치했다.
- 데이터 조회·바인딩과 같은 파일의 태블릿 전용 표는 변경하지 않았다. 현재 제품 diff는 `bottom.jsp` 1파일 `+4/-8`이며 `git diff --check`와 표 구조 검사를 통과했다.
- 원본 작업트리 빌드는 실행 중인 8081 개발 서버가 `build/resources/main/messages/message-common.properties`를 점유해 `processResources` 정리 단계에서 실패했다. 서버를 종료하지 않고 D: 임시 worktree에 동일 패치만 적용해 `gradlew build -q --max-workers=4 --no-daemon`을 통과했으며 임시 worktree와 패치는 제거했다. 기존 Lombok `@Builder` 경고 1건과 deprecated/unchecked 안내는 이번 JSP 변경과 무관하다.
- 기존 통합 stash `d2c8a638e1f508bd4672b47fc191245061c2c02b`와 독립 KAN-63·KAN-6 stash는 원본 보존용이지만 재확정·범위 밖 화면 변경을 포함하므로 최종 적용본으로 사용하지 않는다.

## 2026-08-11 bottom.jsp 전체항목 테스트 데이터

- 사용자 지시에 따라 배지 표시는 아이콘 수령 전까지 보류하고, 진행상태에서 `가공확정`을 제외하는 제품 로직도 이번 범위에서 변경하지 않았다. 테스트 데이터 자체에는 `S53R14`를 넣지 않았으며 기타 디바이스의 작업상세정보 팝업은 `bottom.jsp` 완료 후 별도 도입하기로 했다.
- 개발 DB에 작업지시번호 `QA-ITEM-DETAIL-FULL-001`인 전용 작업상세 데이터 1건을 생성했다. `inspection.selectCommItemDetailInfo`와 같은 컬럼·함수를 사용한 검증에서 데스크톱 표 24개 표시값이 모두 채워졌고 검색 일치 건수는 1건이었다.
- 진행상태 표시는 `출하`, 원본 파트 상태는 `S53R46`, `TBL_CONTROL_PART_PROGRESS`의 `S53R14` 이력은 0건이다. 수량은 8, 도면번호는 `QA-FULL-DWG-001`, 재고번호는 `QA-STOCK-FULL-001`이다.
- `%테스트`에 전용 seeder `seeder/golden/item_detail_full.py`를 추가하고 커밋 `7c21ad6`을 `origin/claude`에 push했다. `ensure`는 중복 없이 기존 1건을 재사용하고, `verify`는 24개 값과 상태를 검사하며, `cleanup`은 전용 anchor 종속 행만 역순 삭제한다.
- cleanup으로 최초 생성분을 삭제한 뒤 다시 생성해 정리 경로를 검증했고, 현재는 재생성한 1건이 개발 DB에 남아 있다. `%테스트` 정규 테스트는 205건 통과했다. 인자 없이 저장소 전체를 대상으로 실행한 `pytest`는 기존 `archive/poc/test_poc.py`의 `poc` import 경로 문제로 수집 단계에서 중단되므로 `python -m pytest tests -q`를 정규 검증 명령으로 사용했다.
- 제품 저장소 변경은 종전과 동일하게 `bottom.jsp` 1파일뿐이며, `bottom.js`, 배지, 상태 필터, 태블릿·TV·POP·출고 등 기타 화면은 변경하지 않았다.

## 2026-08-11 R14/R16 제거 구현·실증

- 제품 저장소 현재 변경은 `bottom.jsp`를 포함한 14파일 `+135/-211`이다. 작업확정/취소 상태 생성, 소재주문, POP, 도면, MCT, 검사/품질, 메인/현황판의 활성 핵심 경로를 `S11R10/S11R20` 기준으로 변경했다. 커밋·push는 하지 않았다.
- `%테스트`에 `seeder/golden/process_without_confirm.py`를 신규 작성했다. 활성 사용자·수주·소재 프로필·외주업체를 매 실행 동적으로 선택하고 `QA-NOR14-` 전용 데이터 7건을 멱등 생성·검증·정리한다. 현재 파일은 미추적 상태다.
- 변경 전 기준선 7개 분기는 1 PASS/6 FAIL, 변경 후 7개 비교 분기와 폐지 엔드포인트·현황판·메인 목록을 합친 10개 항목은 10 PASS/0 FAIL이다. 최종 전용 데이터의 R14/R16 현재값·진행 이력은 0건이다.
- `QA-ITEM-DETAIL-FULL-001`은 작업상세 응답 필드 24/24, R14 이력 0건을 유지한다. `%테스트` 정규 회귀는 205 PASS다.
- 제품 검증은 XML 9개 파싱, 변경 JS 구문, `gradlew classes`, `git diff --check`를 통과했다. 전체 `gradlew build`는 이번 변경과 무관한 기존 `SystemServiceImplTenantCodeUseTest`의 누락 메서드 참조 6건으로 `compileTestJava`에서 실패한다.
- 변경 전/수정/변경 후 보고서 정본은 `C:\Users\User\Desktop\PR\[08.11 가공확정 상태 제거 분기 검증]\`의 동명 MD·자체완결 HTML 두 파일이다. Sites 게시는 요청하지 않아 수행하지 않았다.

## 2026-08-11 추가 경로 이관·검증

- 제품 변경 범위를 v1·TV·리포트·폐지 레거시까지 확장했다. 현재 제품 작업트리는 31파일 `+218/-325`이며 `src/main`의 `S53R14/S53R16` 직접 참조는 0개 파일 0줄이다. 제품 커밋·push는 하지 않았다.
- 실제 개발 서버에서 확장 19경로를 실행한 최종 결과는 18 PASS/0 FAIL/1 BLOCKED다. v1 메인·공정·소재·MCT/CAM·검사, TV MCT/POP, 월별·작업지시 리포트, 폐지 레거시 확정/취소 목록 4종이 통과했다.
- 차단 1건은 v1 도면보드다. 쿼리가 `E07.REF_CD` 공장영역 매핑을 요구하지만 현 개발 DB의 활성 E07 코드 14개 모두 REF_CD가 비어 있어 실제 행 검증용 데이터를 안전하게 구성할 수 없다. 코드의 R14 조건 제거와 XML 파싱은 완료했다.
- 최종 핵심 재실행은 10/10 PASS, 전용 7건의 R14/R16 현재값·이력 0건, 작업상세 24/24, `%테스트` 205 PASS다. 제품 변경 XML 20개 UTF-8 파싱, JS 구문, `gradlew classes`, `git diff --check`도 통과했다.
- 전체 `gradlew build`는 기존 `SystemServiceImplTenantCodeUseTest`가 제품에 없는 `upsertTenantCodeUse/updateTenantCodeUse`를 참조하는 6개 컴파일 오류로 계속 실패한다. 이번 변경에서 범위를 넓혀 수정하지 않았다.
- DB 실측: 현 테넌트 현재 R14(가공확정) 157/R16(가공확정취소) 82(합계 239), 이력 R14 573/R16 196(769). 부모 상태별 현재값은 R14/S11R10(확정) 145, R14/S11R20(취소) 8, R14/S11R50(보류) 4, R16/S11R10 20, R16/S11R20 62건이다. 전 테넌트 현재 2,214, 이력 8,207이다. BASIC 기준코드는 둘 다 `DEL_YN='N'`이고 현 테넌트 `TBL_TENANT_CODE_USE` 행은 0건이다.
- S11R10(확정) 안의 R16 20건은 20개 작업지시에 걸쳐 있고, 19개는 모든 파트가 R16이지만 1개는 R16과 다른 파트 상태가 섞여 있다. 혼합 1건은 부모 상태만으로 손실 없이 이관할 수 없으므로 전체 작업지시 취소와 해당 파트 재활성 중 사용자 업무 결정이 필요하다.
- `SF_GET_CODE_NM`이 `DEL_YN='N'`만 읽고 `SF_GET_PART_STATUS_NM`이 이를 호출하므로 BASIC 코드를 바로 삭제 처리하면 과거 이력의 상태명이 공백이 된다. 현재값 이관·이력 표시 보존·테넌트별 비활성화 구현을 먼저 결정해야 하며, 승인 없이 DB 변경은 수행하지 않았다.

## 2026-08-11 PR 설명 분할

- 현재 제품 31파일 변경을 중복 없이 네 PR 단위로 분류했다: PR-1 상태 전환·레거시 폐지 7파일 +64/-139, PR-2 공통 작업상세정보 1파일 +4/-8, PR-3 핵심 생산 흐름 10파일 +89/-109, PR-4 v1·TV·리포트 13파일 +61/-69.
- 사용자 지정에 따라 한 폴더 <code>C:\Users\User\Desktop\PR\[08.11 가공확정 상태 제거 PR 분할]\</code>에 PR별 MD 정본과 자체완결 HTML을 각각 두어 총 8파일로 만들었다. 이 다중 파일 구성은 기본 작업별 2파일 규칙보다 사용자의 한 폴더 분할 요청을 우선한 예외다.
- 각 문서는 브랜치·기준 HEAD·변경량, 파일별 변경, 설계 원칙, 동작 방식, 검증·재현, 후속 계획, 주의, 복구 지점을 자체완결형으로 담는다. Sites 게시는 요청하지 않아 수행하지 않았다.
- 병합 권장 순서는 PR-1 → PR-3 → PR-4이며 PR-2는 독립 리뷰 가능하다. 로컬 origin/main(7fd03e2)이 현재 기능 브랜치 HEAD 2aad3db의 조상이 아니므로 실제 PR 생성 전 최신 main 동기화·충돌 검토가 필요하다.

## 2026-08-12 통합 코드 가이드 추가

- 기존 PR 분할 폴더에 `[08.11 가공확정 상태 제거 코드 가이드].md` 정본과 자체완결 HTML을 추가했다. 폴더는 PR 설명 4쌍과 코드 가이드 1쌍, 총 10파일이다.
- 가이드는 S11/S53 상태 역할, 작업확정 요청부터 ServiceImpl·MyBatis까지의 저장 흐름, 작업확정 일시 공급원, NULL 파트 조회 규칙, POP·도면 게이트, 첫 POP 소재 상태, v1·TV·리포트 이관 패턴, 병합 순서와 검증 명령을 실제 diff에 맞춰 설명한다.
- 조립/파트의 중요한 범위를 명시했다. 부모 작업지시는 `CONTROL_SEQ` 단위로 확정되지만 파트 NULL 초기화는 화면에서 선택해 요청한 `CONTROL_SEQ + CONTROL_DETAIL_SEQ` 행만 대상으로 하며, 소속 파트 전체 일괄 초기화는 현재 PR 범위가 아니다.
- 문서 검증은 MD·HTML 13개 대단원/17개 하위 단원 일치, 자체완결 HTML 외부 자산 0건, 표 6개 래핑, UTF-8 BOM 없음·대체문자 0건을 확인했다. 제품 작업트리는 기존 31파일 `+218/-325` 그대로이며 R14/R16 직접 참조 0건, 변경 XML 20개 파싱, `git diff --check`를 재확인했다.

## 2026-08-12 bottom.jsp 진행상태 배지

- `status-badge-final.html`을 기준으로 개발 DB의 BASIC 정본을 대조해 S53 22종과 파트 상태가 없을 때 사용하는 S11 5종, 총 27개를 `material/proc/out/done/hold/danger/wait` 7개 배지 그룹으로 매핑했다.
- `inspection.selectCommItemDetailInfo`가 원본 `B.PART_STATUS`를 함께 반환하고, `bottom.js`는 `PART_STATUS`를 우선하며 비어 있을 때만 `CONTROL_STATUS`로 후퇴한다. 상태 문구는 기존 `PART_STATUS_NM`을 유지해 다국어·테넌트 이름 정책을 보존하고, 알 수 없는 테넌트 확장 코드는 기존 텍스트로 표시한다.
- 배지는 KAN-6 범위인 데스크톱 `bottom.jsp` 진행상태 셀에만 적용했다. `S53R14/R16`은 기존 데이터 표시용 배지 매핑에만 존재하며 상태 생성·판정·게이트 의존을 다시 만들지 않는다.
- CSS는 참조 파일과 같은 크기·색상으로 `bottom-itemDetail.css`에 추가했다. JS 문법, XML 파싱, 27/27 매핑, UTF-8/BOM·EOF, `git diff --check`, `gradlew classes`, 전용 데이터 24/24를 확인했다.
- 로컬 프로필 임시 서버의 실제 DOM에서 `S53R46 → detail-status-badge--done`, 배경 `#1f8a4c`, 흰색 글자를 확인했다. 자동 검증의 마지막 한글 기대 문자열 비교만 PowerShell 파이프 인코딩으로 exit 1이었고 출력된 DOM 값은 `출하`로 일치했다. 임시 서버와 로그는 정리했다.
- 2026-08-12 재검증에서 기존 8081 응답에 `PART_STATUS`가 없고 모든 예시가 `CONTROL_STATUS=S11R10`으로 후퇴해 초록색으로 표시되는 현상을 확인했다. 로컬 프로필·8081 인자로 개발 서버를 재시작해 최신 `inspection.xml`을 로드했고, API가 대표 데이터 5종의 `PART_STATUS`를 정상 반환한다.
- 실제 팝업 DOM에서 `material/proc/out/done/wait` 5색을 대표 데이터로 확인했고, 개발 DB에 예시가 없는 `hold/danger`도 렌더러 입력으로 색상을 확인했다. `bottom.js`는 `PART_STATUS` 필드 자체가 누락된 구버전 응답을 `CONTROL_STATUS`로 오인하지 않고 기존 텍스트로 표시하도록 보완했다.
- 전체 `gradlew build`는 기존 `SystemServiceImplTenantCodeUseTest`의 누락 메서드 참조 6건으로 `compileTestJava`에서 실패했고 이번 범위에서는 수정하지 않았다.

## 2026-08-12 단건 정상 프로세스 목업 데이터

- 개발 DB에 작업지시번호 `QA-NOR14-MOCK-001`, 도면번호 `QA-NOR14-MOCK-DWG-001`, 바코드 `QN14MOCK01`인 단품·내부가공 데이터 1건을 생성해 유지했다. 생성된 키는 `CONTROL_SEQ=21134`, `CONTROL_DETAIL_SEQ=21134`다.
- 초기 부모 상태 `S11R20(확정취소)`와 파트 상태 NULL로 격리 데이터를 만든 뒤 실행 중인 애플리케이션의 실제 `/managerControlStatus`를 호출했다. HTTP 200·업무 flag false를 받았고 최종 부모 상태는 `S11R10(확정)`, 파트 상태는 NULL이며 S11R20/S11R10 작업지시 이력이 각각 존재한다.
- 파트 진행 이력은 0건이고 R14/R16 현재값·이력도 0건이다. 바코드와 `TBL_CONTROL_PART_ORDER`·`TBL_CONTROL_PART_ORDER_MAIN` 연결은 각각 1건이다.
- 비변경 확인으로 `popMapper.selectInsertPopPartStatus`를 조회해 작업확정 게이트 통과와 첫 POP 예상 상태 `S53R24(소재입고-재고)`를 확인했다. 메인 가공전 목록과 공정 현황판 `CONFIRMED` 목록에도 해당 작업이 노출된다. POP 저장 요청은 보내지 않아 사용자가 후속 공정을 직접 시험할 수 있다.
- 추가 확인에서 `inspection.selectCommItemDetailInfo`의 `PART_STATUS_NM`이 NULL로 반환됐다. `bottom.js`의 `renderItemDetailStatusBadge()`는 상태명이 비면 셀을 비우고 종료하므로 이 목업의 작업상세 진행상태가 공백으로 표시된다. 확정·POP 등 기능 흐름에는 영향이 없지만 KAN-6 표시에는 영향이 있으며, 해결하려면 파트 상태가 NULL일 때 S11 상태명을 반환하도록 조회를 보완해야 한다. 사용자가 표시 결함 수정까지 요청하기 전에는 제품 코드를 변경하지 않는다.

## 2026-08-12 NULL 후속작업 취소 복귀 검증·수정

- 실제 개발 서버와 동적 격리 데이터를 사용해 `PART_STATUS=NULL`에서 CAM 및 MCT 작업을 시작한 뒤 취소했다. CAM은 `NULL → S53R26(CAM 설계)`, MCT는 `NULL → S53R28(가공중)` 전이는 정상이다.
- CAM 취소는 방금 생성한 S53R26 이력과 TBL_CAM 행을 삭제했지만 `TBL_CONTROL_PART.PART_STATUS`는 S53R26으로 남았다. MCT 취소도 S53R28 이력 삭제와 TBL_MCT_WORK의 S26R40/DEL_YN=Y 처리는 성공했지만 현재 파트 상태는 S53R28로 남았다.
- 원인은 두 취소 경로 모두 신규 상태 이력을 삭제한 뒤 남은 마지막 `TBL_CONTROL_PART_PROGRESS` 행과 현재값을 매핑하기 때문이다. 이전 이력이 한 건도 없으면 UPDATE 조인 대상이 없어 현재값을 NULL로 돌리지 못한다.
- 재확정 형태도 별도로 실증했다. 현재값은 NULL이지만 과거 S53R20(소재입고) 이력이 남은 데이터에서 CAM 시작·취소를 실행하자 최종 현재값이 NULL이 아니라 과거 S53R20으로 복원됐다.
- MCT 첫 실행에서 테스트 파트의 `IMG_GFILE_SEQ=NULL` 때문에 알림 생성부 `DrawingBoardController.getNotificationMessage():611`이 NPE를 냈다. 유효 이미지 참조를 동적으로 연결한 정상 데이터 조건으로 재실행해 시작·취소 HTTP 200 JSON 응답과 동일한 상태 복귀 결함을 다시 확인했다. 이는 취소 상태 결함과 분리된 테스트 데이터 조건이며 제품 코드는 수정하지 않았다.
- CAM의 `machine.beforeStatusControlPart`와 MCT 취소 전용 `drawingMapper.restoreCancelMctControlPartStatus`를 최근 `S11R10(작업확정)` 이후의 마지막 파트 이력 기준으로 변경했다. 해당 구간에 이력이 없으면 LEFT JOIN 결과를 이용해 `PART_STATUS=NULL`과 작업확정 시각으로 복귀하며, 재확정 이전의 과거 이력은 복귀 후보에서 제외한다. MCT 시작·완료의 공용 상태 매핑은 유지하고 취소 서비스만 전용 쿼리를 호출한다.
- MariaDB `EXPLAIN`, XML 파싱, `compileJava`, `assemble`, `git diff --check`를 통과했다. 트랜잭션 롤백 검증에서 최초 NULL은 NULL, 재확정 이전 S53R20 이력은 무시하고 NULL, 작업확정 이후 S53R20 이력은 S53R20으로 복귀했다.
- 새 코드로 8081 개발 서버를 재기동해 실제 API를 다시 호출했다. CAM은 `NULL → S53R26 → NULL`, MCT는 `NULL → S53R28 → NULL`로 통과했고 MCT 작업행도 S26R40/DEL_YN=Y로 정상 취소됐다. 생성한 QA CAM/MCT 작업·로그는 제거하고 `QA-NOR14-CONFIRM` anchor를 기준 상태로 복원했다.
- 전체 `gradlew build`는 이번 변경과 무관한 기존 `SystemServiceImplTenantCodeUseTest`의 누락 메서드 참조 6건 때문에 `compileTestJava`에서 실패했다. 운영 코드와 리소스를 묶는 `assemble`은 통과했으며 이 기존 테스트 결함은 현재 범위에서 수정하지 않았다. DB 스키마 변경과 DDL은 없다.

## 2026-08-12 과거 KAN-6·KAN-63 stash 폐기

- 사용자 지시에 따라 과거 KAN-6 다중 화면 상태 제거 `942c7f7ad80b161f5609fa34f24d4f9f1d7ff1aa`, KAN-63 작업지시 재확정 예외 `ca1524ddac4f19964336418d77f0344ee62a6ec4`, 두 변경의 통합 중복본 `d2c8a638e1f508bd4672b47fc191245061c2c02b`을 stash 목록에서 삭제했다.
- 통합본의 7개 파일은 독립 KAN-6·KAN-63 stash의 대응 파일과 Git blob이 모두 동일해 고유 변경이 없었다. KAN-6 보관본은 현재 확정된 데스크톱 `bottom.jsp` 범위와 다르고, KAN-63 재확정 예외는 본래 Jira 요구가 아닌 것으로 철회된 범위다.
- 삭제 후 `%클라우드` stash는 6개이며 현재 `feature-control-0713` 작업트리의 tracked 변경 34파일은 그대로 유지된다. 남은 작업은 번호가 바뀔 수 있으므로 계속 객체 ID를 정본으로 식별한다.

## 업무용 PC 반영 (2026-07-27)
- 개인 PC 전달본의 `create_control.jsp`, `control-manage.js` 재확정 예외를 업무 PC 작업트리에 병합했다.
- 전달 전후 diff는 기능 변경 `+23/-2`뿐이었고, 업무 PC 병합본은 줄바꿈 형식을 정규화해 비교했을 때 전달본과 내용이 일치한다.
- `git diff --check`와 `node --check control-manage.js`는 통과했다.
- Gradle 빌드는 JDK 21, `--max-workers=4 --no-daemon` 조건으로 재실행해 통과했다.
- 2026-07-27 업무 PC에서 `local` 개발 서버와 DB 연결을 확인하고 기존 작업지시확정 자동화 `CD-FT-CTL-032`를 통과했다. 개인 PC 원본 증적이 없어 수정된 두 화면에서 허용·차단 조건을 네 가지로 다시 구성했으며 모두 통과했다. 검사 중 실제 `/managerControlStatus` 저장 요청은 전송하지 않았다. 증적은 `logs/가공확정_업무PC재검증_2026-07-27.json`이다.
- POP 바코드 스캐너로 직접 입력하는 실기기 검증은 연결된 장비를 식별하지 못해 미실행이었으며, 2026-07-29 사용자 결정으로 보류했다.
- GitHub 인증 후 개인 PC 제품 HEAD `c0ac256`이 `origin/main`에서 확인되어 업무 PC `feature-control-0713`을 fast-forward했다. 기존 staged/unstaged 변경과 미추적 파일 목록은 그대로 복원했고, 당시 tracked 상태 안전 백업 객체 `220f21ff82a14bbeecc86caf343d10e7056b9d35`를 만들었다. 2026-08-10 파일별 patch-id 대조에서 DB결함 4파일은 커밋 `88bf7dc`, 단가검토 3파일은 stash `24c94b4db0dfafebf70f39d1bd2b0940bc8a376d`, KAN-63 2파일은 stash `ca1524ddac4f19964336418d77f0344ee62a6ec4`와 모두 일치하고 고유 변경이 0개임을 확인해 사용자 지시에 따라 중복 안전 백업을 삭제했다.

## 블로커·주의
- DB 변경(메뉴 숨김, 이력 보완)은 PROJECT_RULES.md DB 규칙: 백업+트랜잭션+검증 SELECT 후 사용자 확인받고 COMMIT.
- %클라우드 커밋은 사용자 몫. 현재 제품 변경은 `feature-control-0713` 작업트리의 34파일 `+357/-329`이고, `%테스트` 동적 분기 하네스는 미추적 1파일이다. 폐기된 범위의 과거 가공확정 stash 3개는 2026-08-12 사용자 지시에 따라 삭제했다.
- 조사 범위 = 가공확정/S53R14 이웃. 전 시스템 전수 감사 아님.
- DB 접속 확인됨(읽기 조회용): D:/Tool/mariaDB/bin/mysql --skip-ssl -h121.165.20.66 -utomes -p*** smd (SSL 미지원 서버라 --skip-ssl 필수).

## 관련 파일·경로
- OrderServiceImpl.java(managerControlStatus 236-301, updateControlConfirmProcess 646-692), OrderController.java:116
- PopServiceImpl.java(103/109/177-186, 128), pop.xml(172,179-182), tomes.xml(207,217)
- MaterialServiceImpl.java(308-356,410-443), material.xml(887-894,1343,1381), DataBaseConfig.java:59
- control-manage.js(1896,~1859), process-confirm.jsp, confirm/work_confirm.jsp, attr/sample/body-menu.jsp:35, pages/admin/menu-master.jsp
- v1/material.xml, v1/main.xml:114, process.xml, machine.xml, process_dashBoard.jsp:689
- 지라: KAN-63/6/35/38. 회의용 아티팩트: claude.ai/code/artifact/950fff0e-555d-4390-a48b-b1e1c54e83d0
- 과거 논의 원본: 2026-07-21 세션 트랜스크립트(f3a45bd5...jsonl) — 별도 문서로 안 남겨졌던 것을 이 파일로 정본화.
