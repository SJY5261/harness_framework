# 거래명세표 — 상세 인수인계

_2026-07-14 HANDOFF.md 작업별 분리로 생성. 범위: 거래명세표 팝업 리뉴얼(슬라이드36~39)·라벨 출력 모달. 섹션 최신순, 원문 그대로 이동._

## [2026-07-20] stash 재구성 — 거래명세표 전용 stash로 통합 (사용자 지시·승인)
- 사용자 지시("stash에 거래명세표만 남기고 나머지 꺼내기")로 %클라우드 stash 재구성 완료:
  - 신규 `stash@{0}` = "On Danga-order: 거래명세표 슬라이드36+라벨모달 (2026-07-20 재구성, base 3b52126)" — transaction_statement.jsp 단독(142+/9-). 구 stash(7/8)의 해당 diff를 현 HEAD(3b52126 publishing_2606 머지)에 3-way 적용해 생성, **충돌 없음**(상류 42d4f2c·665e504 반영된 베이스라 재개 시 바로 apply 가능).
  - 구 stash 2건은 drop됨(사용자 승인): ⓐ7/15 DB결함 2파일 — 내용은 워킹트리에 이미 존재(채번 락 메서드 13곳·order.xml 신규 쿼리 3건 grep 확인) ⓑ7/8 4파일 — 견적 3파일 분은 커밋 6fcfcd8(페이지 최신본)·현 워킹트리로 대체 확인.
  - 복구용 백업 패치: `&하네스\logs\stash_backup_2026-07-20_stash0_dbdefect.patch`(37KB) / `stash_backup_2026-07-20_stash1_4files.patch`(25KB) (+기존 `cloud_local_changes_backup_2026-07-08.patch` 병존).
  - 주의: 구 stash의 `estmateCalculate.xml`(비v1) 1줄(SF_GET_ETC_NM 3인자)은 현 파일에 없어 꺼냈다가, 단가검토.md의 "사용자 의도적 원복(2인자 유지)" 결정 확인 후 즉시 되돌림 — 비v1 estmateCalculate.xml은 워킹트리 clean(diff 0줄). ※`v1/estmateCalculate.xml`이 M(94줄)인 것은 별건인 단가검토 미커밋분이니 혼동 금지. 3인자 수정분은 필요 시 백업 패치에 남아 있음.

## [2026-07-03] 거래명세표 팝업 슬라이드36 반영 (%클라우드 Danga-order 워킹트리, 미커밋)
### 배경/기준
- 라벨출력 홀딩 해제, 설계 기준 = `Downloads\고도화 및 수정 화면설계서_260629_01.pptx` 슬라이드 36~39 (아래 홀딩 섹션의 2026-07-03 갱신 참조). 이번 턴은 **슬라이드 36(거래명세표 생성 화면)만** 반영. 사용자 확정: 기존 라벨 출력 버튼 동작 유지, 신규 라벨팝업은 라벨수량 컬럼 클릭 진입(슬라이드 39 단계에서 구현).
- 브랜치: 브랜치 질문 60초 무응답 → 깃 상태 변경 없이 **Danga-order 워킹트리에서 파일 수정만** 진행(커밋·브랜치 정리는 사용자 몫).
### 변경 (`common/transaction_statement.jsp` 1파일, answer-reviewer 검증 OK)
- 버튼: "영업도면출력"→"납품도면 출력"(id SALES_DRAWING_PRINT·핸들러·/makeSalesDrawingPrint 불변 — **납품도면 전용 엔드포인트 없음**, PdfPrintMakeController 매핑 6개 확인. 명칭만 변경으로 해석). 엑셀 아이콘(img)→popupBtn 텍스트 "거래명세서 출력"(id TRANSACTION_STATEMENT_EXPORT·기존 엑셀 다운로드 핸들러 유지 — **출력 팝업 배선은 슬라이드 38 단계**).
- colModel: 도면 아이콘(IMG_GFILE_SEQ)을 도면번호 뒤로 이동(목업 순서), 공급단가/금액계 width 130(④넓힘), '포장수량'→'라벨수량'(width 60, dataIndx PACKING_CNT·editable 유지 — 클릭 진입은 39 단계), '비고'→'거래명세표 비고'(width 130/minWidth 110), 규격 minWidth 85(autoFit 압축 잘림 방지).
### 검증 (라이브 8081, Playwright, 세션 scratchpad verify_ts_popup.py)
- 수주관리 실진입(기본 검색조건 0건 → 시작일 2020-01-01로 넓혀 재검색 100건), INV 없는 행 선택 → 거래명세표 생성 팝업 실오픈. 버튼 텍스트/표시 상태(삭제 hidden), 컬럼 순서·타이틀·잘림 없음 확인, JS 에러 0. 캡처 ts_popup_after.png. **주의**: 수주관리 화면도 탭 래퍼 display:none 플레이크 있음(수주현황과 동일 — 강제표시+JS 클릭으로 우회).
### 남은 것 (사용자 결정/후속)
- ①기본정보 '제목'(INVOICE_TITLE) 행: 목업(image54/58)엔 없음. 저장 로직이 폼 전체 수집으로 참조하므로 **유지**해둠 — 제거 여부 사용자 결정 필요. ②삭제 버튼명 "거래명세표 삭제"(슬라이드 37) 미적용. ③거래명세서 출력 팝업(38) 미구현.

### [2단계, 같은 날] 라벨 출력 모달 화면 + 라벨수량 클릭 배선 (같은 파일, 미커밋, answer-reviewer 검증)
- **모달 마크업**: `popup_container#LABEL_PRINT_POPUP > layerPopup(620px)` — X(절대배치)/tableL 정보 4행(접수번호·도면번호·발주번호·출하대상수량+포장(라벨)수량)/분할테이블(No·수량·출하실적+헤더 추가·전체출력 버튼, 행별 제거(red)·출력)/하단 닫기·저장(green). 전용 style 블록은 #LABEL_PRINT_POPUP 스코프. 열고닫기 `.modal('show'/'hide')`(bottom.js popup_container 관례, 이 JSP는 bottom.jsp include).
- **라벨수량 컬럼(PACKING_CNT)**: editable **true→false**, 밑줄 링크 render(null→0), 클릭=fnOpenLabelPrintPopup(rowData). **이 그리드에서 포장수량 직접 편집 더 이상 불가**(설계 의도 — 편집은 라벨 모달로 이동).
- **필드 임시 매핑(추론, 확정 필요)**: 접수번호=`REGIST_NUM (CONTROL_ORDER_QTY)`, 출하대상수량=CONTROL_ORDER_QTY(목업 "260616-0005 (15)"의 15=출하대상수량 대응), 포장(라벨)수량=PACKING_CNT.
- **동작 범위**: 추가(로컬 행 append, 수량 input)/제거(행 삭제+No 재부여)/닫기만 동작. **출력·전체출력·저장은 미배선(no-op)** — 분할 데이터 조회/저장/출력은 다음 단계(TBL_OUT_BARCODE 수량 컬럼 부재 문제 포함).
- 검증(라이브 8081, verify_label_modal.py): 링크 밑줄·클릭 오픈, 필드 5개 채움, 추가x2→No 1,2·제거→재부여, 닫기·재오픈 시 행 초기화, JS에러 0. 캡처 label_modal.png — 목업 레이아웃 일치(X 우상단·헤더 버튼 정상. float X·버튼 찌그러짐 2회 보정함).
- **★브랜치 주의**: 오늘 10:03 reflog에 `Danga-order → feature_drawing_popup_260702` checkout 있음(**세션이 실행한 것 아님** — 사용자 추정). 현재 체크아웃 = feature_drawing_popup_260702, 미커밋 4파일(transaction_statement.jsp + estimate 3파일)은 워킹트리에 유지 중. 거래명세표 변경 커밋 시 브랜치 확인 필요.

## [2026-07-02] 거래명세표 "라벨 출력" 모달 — 분석만, 구현 홀딩 (코드 무변경)
### [2026-07-03 갱신] 기획 변경 — 설계서 슬라이드 36~39로 대체
- 사용자 통지: 기준이 스크린샷(084032.png)에서 **`Downloads\고도화 및 수정 화면설계서_260629_01.pptx` 슬라이드 36~39**로 변경됨. 슬라이드-이미지 매핑(media 번호 아님, 슬라이드 rels 실측): 36=image54(생성화면)+55(그리드행)+57(출력확인알림)+56, 37=image58(상세보기), 38=image59(거래명세서 출력), 39=image56(라벨팝업)+60(저장알림)+61(전송완료알림). 추출본: 세션 scratchpad `pptx_label\deck\ppt\media\`.
- 요지: 라벨 모달 단건이 아니라 **거래명세표 팝업 전체 리뉴얼** 4장. ①생성/상세보기 버튼 재구성(영업도면 출력→납품도면 출력, 엑셀 아이콘→거래명세서 출력 버튼, 상세보기엔 거래명세표 삭제(빨강)+수정 추가) ②그리드에 라벨수량(밑줄 숫자 클릭=라벨팝업)·납품확인·거래명세표 비고 컬럼, 공급단가/금액계 폭 확대 ③거래명세서 출력 팝업(템플릿 select+PDF/Excel 저장+인쇄하기+우측 미리보기, 납품확인용 바코드 삭제) ④라벨 팝업: 접수번호 **radio 아닌 단일 텍스트**, 타이틀 '포장수량'→'포장(라벨)수량', 분할테이블(No/수량/출하실적/제거/출력 + 헤더에 추가/전체출력), **저장은 출력 안 함** 명시. 진입=라벨 출력 버튼 or 라벨수량 숫자 클릭.
- 어제 미해결 결정사항 대비: ⑤다중 접수번호 radio UI는 설계에서 소멸(단일 표시). ①수량 의미(추가/제거 버튼 있어 편집형으로 보임 — 라벨별 수량 저장이면 TBL_OUT_BARCODE에 수량 컬럼 없음 문제 여전) ②PACKING_CNT 이원화 ③출하 라벨 보호 ④#ORDER_MANAGE_LABEL_PRINT 포함 여부는 여전히 미확정.
### [2026-07-03] 홀딩 사유 확인됨 + 진입점 정책 확정 (사용자 설명)
- **홀딩됐던 "기능적 이슈"의 정체**: 기존 라벨 출력 버튼은 발주사가 같은 **여러 접수를 한 번에** 라벨 출력하는데, 신규 모달은 접수번호를 **하나만** 반영할 수 있어 다중 접수 케이스가 깨질 소지였음.
- **확정 방향**: ⓐ기존 "라벨 출력" 버튼 기능은 **그대로 유지**(fnConfirm→전량 출력, 다중 접수 커버) ⓑ신규 모달은 **라벨수량 컬럼(행 단위) 클릭으로만** 진입하는 추가 기능. 즉 설계서 39의 "라벨 출력 또는 라벨수량 숫자 클릭" 중 버튼 쪽은 기존 동작 유지로 해석(모달 아님).
### 상태
- **홀딩 해제 방향 논의 중** (2026-07-03, 위 진입점 정책 확정). 코드·DB 일절 변경 없음(분석/조회만). 아래 분석 재사용.
### 요구 (홀딩된 기획)
- 바탕화면 `스크린샷 2026-07-02 084032.png`의 "라벨 출력" 모달 구현. 흐름: 수주관리 → 행선택 → 거래명세표 팝업(TRANSACTION_STATEMENT 버튼 or INV No. 클릭) → 라벨 출력 버튼 → (현재 fnConfirm 확인창 대신) 모달 open.
- 모달 구성: 접수번호 radio / 도면·발주번호 / 출하대상수량·포장(라벨)수량 / 분할 테이블(No·수량·출하실적·제거·출력) / 추가·전체출력 / 닫기·저장.
### 현재 코드 흐름 (검증됨, answer-reviewer OK)
- 팝업 진입 2경로: `order-manage.js:2683`(#TRANSACTION_STATEMENT) / `:596~638`(INV No. postRender) → `window.open('/transactionStatement')` 별창(1250x820). 팝업은 `opener.$orderManagementGrid`로 부모 데이터 접근.
- 라벨 출력: `transaction_statement.jsp:343~386` — 그리드 전체 ORDER_SEQ → `inspection.selectOutgoingLabelType4`(inspection.xml:1805, TBL_OUT_BARCODE) → fnConfirm(N건) → `fnBarcodePrint`(/barcodePrint) 전량 출력. 동일 패턴이 `order-manage.js:2811`(#ORDER_MANAGE_LABEL_PRINT)에도 존재.
### DB 실측 (mysql.exe로 SHOW COLUMNS, 121.165.20.66/smd — --skip-ssl 필요)
- `TBL_OUT_BARCODE`: BARCODE_NUM(PK)/CONTROL_SEQ/CONTROL_DETAIL_SEQ/ORDER_SEQ/PACKING_NUM/DEL_YN+감사컬럼. **라벨별 수량 컬럼 없음.**
- `TBL_OUT`: PACKING_NUM/OUT_QTY/**OUT_DT** 있음 → 라벨행별 "출하실적·수량"은 ORDER_SEQ+PACKING_NUM 조인으로 조달 가능(그릴링 중 사용자 지적으로 "수량=출하실적 표시 전용, 편집 없음" 방향이 유력했음 — 미확정).
### 재개 시 남은 결정 (그릴링 1문항 진행 중 홀딩)
- ①수량 컬럼 의미 확정(표시 전용이면 DDL 불필요) ②PACKING_CNT 이원화(TBL_ORDER vs TBL_CONTROL_PART_ORDER — 그리드는 TBL_ORDER 것, 라벨 생성·출하 검증은 TBL_CONTROL_PART_ORDER 것, 별도 갱신 경로) ③출하된 라벨 보호 정책 ④#ORDER_MANAGE_LABEL_PRINT 포함 여부 ⑤다중 접수번호 radio UI.
- **홀딩 사유인 "기능적 이슈"의 구체 내용은 미청취** — 재개 시 먼저 확인할 것.
