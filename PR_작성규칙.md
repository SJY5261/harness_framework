# PR 작성 규칙 (tomes-cloud 공통)

> 이 문서는 **PR(풀 리퀘스트) 설명을 어떤 구조로 쓸지** 정한 팀 공통 규칙이다.
> 리뷰어가 "무엇을 / 왜 / 어디까지 / 되돌리는 법"을 **스크롤 없이 한 화면에서** 파악하게 하는 것이 목표다.
> 예시 PR: `feature_same_archive_redesign` (유사형태 기록조회 3열 신규 디자인).
>
> **2026-07-21 통합**: 사용자 전달 MD 산출물 포맷(바탕화면 MD·PR리뷰)의 정본도 이 문서다.
> 산출물 형태별 형식 구분은 `8절`, MD 파일 포맷은 `9절`, PR리뷰 저장 구조는 `10절` 참고.
> (기존에 CLAUDE.md와 이 문서에 규칙이 나뉘어 표 형식 충돌·누락이 발생해 이관·통합함)

---

## 0. 가장 중요한 규칙 (먼저 읽기)

1. **긴 설명·세부 내용은 반드시 `<details><summary>제목</summary> … </details>` 안에 접어 넣는다.**
   - `<summary>`(접힌 상태에서 보이는 제목)는 **무조건 있어야 한다.** ❌ `<summary>` 없는 `<details>`는 금지.
   - 이유: PR 본문은 짧고 스캔 가능해야 한다. 깊은 내용은 접어두고, 리뷰어가 필요할 때만 펼친다.
2. **섹션 순서를 지킨다.** (아래 `2. 한눈에 보는 필수 구조` 표의 순서)
3. **필수 섹션은 비우지 않는다.** 해당 없으면 "해당 없음"이라고 적는다.
4. 모든 문장은 **한글**로 쓴다. (커밋 메시지 규칙과 동일)

---

## 1. 헤더 블록 (제목 + 메타 줄)

PR 맨 위에는 **제목 한 줄 + 메타 정보 한 줄**을 둔다.

```html
<h1>유사형태 기록조회 — 3열 신규 디자인 적용</h1>

<p>
  <code>feature_same_archive_redesign</code> → <code>main</code>
  &nbsp;·&nbsp; 커밋 <code>9dc574e</code>
  &nbsp;·&nbsp; 변경 <b>3 files · +1,190 / −1</b>
  &nbsp;·&nbsp; <b>상태: 목업(검증) 단계 — 실제연동 전</b>
</p>
```

메타 줄에 **반드시** 넣을 4가지:

| 항목 | 예시 | 설명 |
|------|------|------|
| 브랜치 방향 | `feature_xxx → main` | 어디서 어디로 머지하는지 |
| 커밋 | `9dc574e` | 대표 커밋 해시(짧게) |
| 변경량 | `3 files · +1,190 / −1` | 바뀐 파일 수 / 추가·삭제 라인 |
| 상태 | `목업 단계` / `실연동 완료` / `WIP` | 지금 어느 단계인지 한 단어로 |

---

## 2. 한눈에 보는 필수 구조

섹션은 **이 순서대로** 배치한다. `필수`는 반드시, `선택`은 해당될 때만.

| # | 섹션 (제목 예시) | 필수? | 형식 | 들어갈 내용 |
|---|------------------|-------|------|-------------|
| — | 헤더 블록 (제목 + 메타 줄) | ✅ 필수 | h1 + p | 위 `1번` 참고 |
| 1 | `개요` | ✅ 필수 | 본문 + 불릿 | 무엇을 / 왜 바꾸는지 2~3줄 |
| 2 | `주요 변경` | ✅ 필수 | 표 | 파일 / 구분(신규·수정·삭제) / 내용 |
| 2-1 | `DB 변경 SQL` | DB 변경 시 ✅ 필수 | **`<details>`** | 대상·변경 전후·실행 순서·실제 DDL/DML·검증·복구 SQL |
| 3 | `설계 원칙` | ✅ 필수 | 불릿 | 이렇게 만든 이유·전제(무수정 원칙 등) |
| 4 | `동작 방식` | 선택 | **`<details>`** | 핵심 로직·임시블록 등 깊은 설명 |
| 5 | `검증 완료 & 테스트 방법` | ✅ 필수 | **`<details>`** | 확인한 것 + 리뷰어 재현 순서 |
| 6 | `후속 계획` | 선택 | 번호 목록 | 이 PR 이후 예정 작업 |
| 7 | `알려진 사항 / 주의` | 선택 | **`<details>`** | 미완성·데이터 이슈·브랜치 베이스 등 |
| 8 | `복구 지점` | ✅ 필수 | 본문 | 문제 시 되돌리는 방법 |

> **`<details>` 권장 섹션(4·5·7)** 은 내용이 길어지므로 접어 넣는 것을 기본으로 한다. 짧으면 펼친 채로 둬도 되지만, 길면 **반드시 `<summary>` 달아 접는다.**

---

## 3. 섹션별 작성 가이드

### 1) 개요 (필수)
- **무엇을 바꾸는지**(대상 화면/기능 + 경로) 한 문장.
- **왜 바꾸는지 / 어떤 방식인지** 불릿 2~3개.
- 현재 단계(목업/실연동/WIP)를 분명히 적는다.

### 2) 주요 변경 (필수)
표로 정리한다. 열은 **파일 / 구분 / 내용** 고정.

```html
<table>
  <thead><tr><th>파일</th><th>구분</th><th>내용</th></tr></thead>
  <tbody>
    <tr><td><code>경로/파일.jsp</code></td><td>신규</td><td>한 줄 설명</td></tr>
    <tr><td><code>경로/파일.java</code></td><td>수정(1줄)</td><td>무엇을 바꿨는지</td></tr>
  </tbody>
</table>
```
- 구분 표기: `신규` / `수정` / `삭제`.

### 2-1) DB 변경 SQL (컬럼·인덱스·제약·프로시저·함수·트리거 변경 시 필수 · `<details>`)

DB 구조나 저장 루틴을 변경한 PR은 리뷰어가 그대로 추적·재현할 수 있도록 다음 내용을 남긴다.

- 대상 DB·스키마·테이블/루틴과 적용 환경을 명시한다.
- 컬럼·인덱스·제약은 **변경 전 → 변경 후** 정의를 표로 비교한다.
- 백업 → 사전 검증 → 데이터 보정 DML → DDL/루틴 교체 → 사후 검증 순서로 실제 적용 절차를 적는다.
- 실행한 `CREATE`, `ALTER`, `DROP`, `RENAME`, `UPDATE`, `CALL` 등의 SQL을 코드 블록에 원문으로 넣는다.
  비밀번호·접속정보·실고객 데이터는 넣지 않는다.
- 프로시저·함수·트리거는 파라미터, `DEFINER`, 트랜잭션/내부 `COMMIT`, 권한 등 유지한 계약과 실제 변경 구문을 함께 적는다.
  전문이 길면 `<details>` 안에 전체 DDL 또는 변경 블록과 원문 보관 경로를 둔다.
- 실제 실행문을 보존하지 못했다면 추정 SQL을 실행문처럼 쓰지 않는다.
  `실행 원문 미보존`을 명시하고, `SHOW CREATE` 등으로 검증한 **최종 상태 재현 SQL**과 근거를 구분해 제공한다.
- 사후 검증 `SELECT`와 영향 행 수, 복구 SQL, 복구 시 데이터 손실·호환성 주의를 함께 적는다.

### 3) 설계 원칙 (필수)
- "왜 이렇게 만들었나"를 불릿으로. (예: *원본 무수정 + `_new` 별도파일*, *CSS 스코프 처리*)
- 리뷰어가 "왜 이렇게 했지?" 하고 의심할 지점을 미리 설명한다.

### 4) 동작 방식 (선택 · `<details>`)
- 핵심 함수 흐름, 임시 목업 블록, 알고리즘 등 깊은 설명.
- **`<summary>`에 무슨 내용인지 + `(펼치기)` 표시.**

### 5) 검증 완료 & 테스트 방법 (필수 · `<details>`)
- **검증 완료**: 직접 확인한 항목 불릿.
- **테스트 순서**: 리뷰어가 그대로 따라 할 수 있는 번호 목록.
- 진입 전제(예: "부모 화면 버튼으로 열어야 함")가 있으면 `blockquote`로 강조.

### 6) 후속 계획 (선택)
- 이 PR에서 **안 한 것**과 다음 작업을 번호 목록으로.
- "이번 PR은 여기까지"의 경계를 명확히 한다.

### 7) 알려진 사항 / 주의 (선택 · `<details>`)
- 미완성 부분, 데이터 이슈(버그 아님), 브랜치 베이스 같은 함정.
- 리뷰어가 오해할 만한 것을 **미리 해명**한다.

### 8) 복구 지점 (필수)
- 머지 후 문제가 생기면 **무엇을 어떻게 되돌리는지** 한 문단.
- 예: "`StaticUrlController` 반환값을 원래 뷰로 되돌리면 즉시 복귀".

---

## 4. `<details>` / `<summary>` 작성 예시 (★필수 규칙)

```html
<details>
  <summary><b>동작 방식</b> &nbsp;— 핵심 로직 설명 (펼치기)</summary>
  <br>
  <p>여기에 접힌 내용이 들어간다.</p>
  <ul>
    <li><code>함수명()</code> — 역할 한 줄</li>
  </ul>
</details>
```

규칙:
- ✅ `<summary>` **필수.** (❌ 제목 없는 접힘 금지)
- `<summary>` 안에는 **굵은 제목 + `(펼치기)`** 형태를 권장.
- `<summary>` 바로 뒤 `<br>` 한 줄로 여백을 준다.

---

## 5. 복붙용 템플릿 (이 블록을 그대로 복사해서 채우기)

```html
<h1>[기능명] — [무엇을 바꿨는지 한 줄]</h1>

<p>
  <code>[브랜치]</code> → <code>main</code>
  &nbsp;·&nbsp; 커밋 <code>[해시]</code>
  &nbsp;·&nbsp; 변경 <b>[N] files · +[추가] / −[삭제]</b>
  &nbsp;·&nbsp; <b>상태: [목업/실연동/WIP]</b>
</p>

<h2>개요</h2>
<p>[대상 화면/경로]을 [어떻게] 바꿉니다.</p>
<ul>
  <li>[방식 1]</li>
  <li>[방식 2]</li>
  <li>[현재 단계]</li>
</ul>

<h2>주요 변경</h2>
<table>
  <thead><tr><th>파일</th><th>구분</th><th>내용</th></tr></thead>
  <tbody>
    <tr><td><code>[경로]</code></td><td>신규</td><td>[설명]</td></tr>
    <tr><td><code>[경로]</code></td><td>수정</td><td>[설명]</td></tr>
  </tbody>
</table>

<details>
  <summary><b>DB 변경 SQL</b> &nbsp;— 변경 전후·실행문·검증·복구 (펼치기)</summary>
  <br>
  <p><b>대상:</b> <code>[환경].[스키마].[테이블 또는 루틴]</code></p>
  <p><b>변경 전 → 변경 후:</b> [정의 비교]</p>
  <pre><code class="language-sql">[백업 SQL]
[데이터 보정 DML]
[DDL 또는 루틴 교체문]
[사후 검증 SELECT]
[복구 SQL]</code></pre>
</details>

<h2>설계 원칙</h2>
<ul>
  <li><b>[원칙 1]</b> — [이유]</li>
  <li><b>[원칙 2]</b> — [이유]</li>
</ul>

<details>
  <summary><b>동작 방식</b> &nbsp;(펼치기)</summary>
  <br>
  <ul>
    <li><code>[함수]</code> — [역할]</li>
  </ul>
</details>

<details>
  <summary><b>검증 완료 &amp; 테스트 방법</b> &nbsp;(펼치기)</summary>
  <br>
  <b>검증 완료</b>
  <ul>
    <li>[확인한 항목]</li>
  </ul>
  <b>테스트 순서</b>
  <ol>
    <li>[1단계]</li>
    <li>[2단계]</li>
  </ol>
</details>

<h2>후속 계획</h2>
<ol>
  <li><b>[다음 작업]</b> — [내용]</li>
</ol>

<details>
  <summary><b>알려진 사항 / 주의</b> &nbsp;(펼치기)</summary>
  <br>
  <ul>
    <li>[주의할 점]</li>
  </ul>
</details>

<h2>복구 지점</h2>
<p>[문제 시 되돌리는 방법]</p>
```

---

## 6. 제출 전 체크리스트

- [ ] 제목 + 메타 줄(브랜치/커밋/변경량/상태) 다 채웠다.
- [ ] 필수 섹션(개요·주요 변경·설계 원칙·검증/테스트·복구 지점) 모두 있다.
- [ ] 긴 내용은 **`<details>` + `<summary>`** 로 접었고, **`<summary>` 제목이 빠진 곳이 없다.**
- [ ] `주요 변경` 표에 바뀐 파일이 빠짐없이 들어갔다.
- [ ] DB 변경이 있으면 대상·변경 전후·실제 DDL/DML·적용 순서·검증 SELECT·복구 SQL을 모두 적었다.
- [ ] `테스트 순서`를 리뷰어가 그대로 따라 할 수 있다.
- [ ] 모든 문장이 한글이다.

---

## 7. 참고: 형식 관련 메모

- GitHub PR 설명은 **HTML 태그를 지원**한다(`<details>`, `<summary>`, `<table>`, `<h2>` 등). 위 예시처럼 HTML로 써도 되고, 표·접힘이 필요 없는 짧은 섹션은 일반 마크다운(`##`, `-`)으로 써도 된다.
- 단, **접힘(`<details>`)·표(`<table>`)는 HTML 태그**를 쓴다(마크다운보다 정렬이 안정적).
- 이모지는 **장식용으로 쓰지 않는다.** 제목·문장은 텍스트만으로 구분한다.
- 단, **의미가 있는 표시**는 허용한다 — 예: ✅(필수/완료/가능), ❌(금지/불가). 이런 신호용 이모지만 최소한으로 쓴다.
- ※ 7절의 표·이모지 규칙은 **GitHub PR 본문에만** 적용된다. 바탕화면 MD 파일은 아래 8·9절이 우선한다.

---

## 8. 산출물 형태별 형식 (2026-07-21 통합 — 표 형식 충돌 해소)

같은 내용이라도 **어디에 올리는 산출물이냐**에 따라 형식이 다르다. 혼동 금지.

| 산출물 | 표 | 접힘 | 이모지 | 기준 렌더러 |
|--------|-----|------|--------|-------------|
| **GitHub PR 본문** | HTML `<table>` (7절) | `<details>`+`<summary>` | 장식 금지, ✅/❌ 신호용만 | GitHub |
| **바탕화면 MD 파일** (PR리뷰 포함) | **일반 마크다운 표** + 상단 `<style>` 블록 (9절) | `<details>`+`<summary>` 동일 | ✅/❌·섹션 이모지 **적극 사용** | VS Code 미리보기 |

- PR리뷰 MD는 **구조(헤더 메타 줄·섹션 순서·접힘·한글)는 이 문서 1~6절**, **표·스타일·이모지는 9절**을 따른다.
- 사고 사례: 2026-07-20 PR리뷰 MD가 7절(HTML 표)만 따르고 MD 포맷(style 블록+마크다운 표)을 누락 — 규칙이 두 파일에 나뉘어 있던 것이 원인. 이 문서로 통합했다.

## 9. MD 파일 포맷 (VS Code 미리보기 기준 — CLAUDE.md에서 이관)

사용자에게 전달하는 MD 파일(바탕화면 MD·PR리뷰 등)은 VS Code 마크다운 미리보기(Ctrl+Shift+V)로
읽었을 때 가독성이 좋아야 한다. 2026-07-09 시행착오 끝에 확정한 형식:

- **파일 상단에 `<style>` 블록 한 개**를 두고, 표는 **일반 마크다운 표**로 작성한다.
  - 미리보기: 마크다운 표가 HTML `<table>`로 렌더링되면서 style 블록이 적용돼 예쁘게 나옴.
  - 소스로 볼 때: 깔끔한 마크다운 표라 읽기 좋음.
  - (인라인 스타일 HTML 표 방식은 소스가 HTML 벽이 돼 폐기 — 2026-07-09)
- 스타일 확정값(아래 템플릿 그대로 사용):
  - 셀 공통 `border:1px solid #c0c8d0` + `padding:8px 12px`
  - 헤더만 `border-bottom:2px solid currentColor` + `background:rgba(127,127,127,0.25)`
    — 반투명 배경·currentColor 선이라 다크/라이트 테마 모두 적응. 고정 색은 다크에서 묻히거나 튀므로 금지.
  - 본문 셀 배경색 없음(줄무늬·강조색 금지), 표 너비 지정 없음(내용 폭 자동, `width:100%` 금지).
- ✅/❌ 체크 아이콘과 섹션 이모지(1️⃣ 📌 💡 등)를 적극 사용해 시각 구분한다.
- 항목 나열·비교·범위 표시는 표로, 서술은 짧은 목록/문단으로.
- 파일은 UTF-8이므로 이모지 사용 무방(콘솔 출력·커밋 메시지의 ASCII 제한과 별개).
- 참고: `<style>` 블록은 VS Code 미리보기용이다. GitHub 등 다른 렌더러는 style을 제거하지만
  마크다운 표 자체는 기본 스타일로 정상 표시되므로 겸용에 문제 없다.

파일 상단 style 템플릿:
```html
<style>
table { border-collapse: collapse; }
th, td { border: 1px solid #c0c8d0; padding: 8px 12px; }
th { border-bottom: 2px solid currentColor; background: rgba(127,127,127,0.25); text-align: left; }
</style>
```

## 10. PR리뷰 저장 구조 (2026-07-21 도입, 2026-07-28 MD·HTML 기본으로 개정)

PR리뷰는 MD 파일 하나만 덩그러니 두지 않는다. **작업별 폴더**를 만들고 MD 정본과 자체완결 HTML을 모두 저장한다.

```
C:\Users\USER\Desktop\PR리뷰\[MM.DD 요약]\        ('/'는 '.'로)
├── [MM.DD 요약].md      <- 정본 (필수, 9절 MD 포맷)
└── [MM.DD 요약].html    <- MD 정본에서 생성한 자체완결 HTML (필수)
```

- **MD가 정본**이다. 아티팩트는 열람용 렌더링이며 내용은 MD와 동일해야 한다.
  내용 수정 시 순서: MD 먼저 수정 → 같은 MD에서 HTML 재생성.
- 자체완결 HTML을 만들었으면 Codex Sites 게시나 `아티팩트.url`을 추가로 만들지 않는다.
- 사용자가 웹 게시를 명시적으로 요청한 경우에만 Codex Sites에 게시하고 `아티팩트.url`을 추가한다.
  이때 기본 접근은 현재 사용자만 허용하는 owner-only이며, 공유·공개 접근은 대상과 범위를 확인한 뒤 변경한다.
- 명시적으로 Sites를 게시할 때는 `.openai/hosting.json`의 `project_id`를 그대로 보존하고 같은 작업 사이트를 중복 생성하지 않는다.
- 에이전트 종류를 이유로 MD만 생성하는 예외는 없다. 기본 완료 조건은 MD 정본과 자체완결 HTML 2종이다.
- 사용자가 다른 기준 경로를 지정하면 그 경로를 우선하되 작업별 폴더와 MD·HTML 2종 구조는 그대로 적용한다.
- 2026-07-20 이전의 폴더 없는 단일 MD 파일들은 소급 변환하지 않는다(요청 시에만).
